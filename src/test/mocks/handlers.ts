import type { HttpHandler } from "msw";

// Populated in full by Task 3 (API DTO types, MSW handlers, and per-role
// Scope fixtures). This stub exists so Task 2's test harness (server.ts ->
// handlers.ts) has a valid import target and the Wave 0 smoke test can run
// green before the real endpoint handlers are authored.
export const handlers: HttpHandler[] = [];
