---
phase: 04-contrats-de-location
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/features/contracts/ContractDetail.tsx
  - src/features/contracts/ContractDetail.test.tsx
  - src/features/contracts/forms/ActivateForm.tsx
  - src/features/contracts/forms/CloseForm.tsx
  - src/features/contracts/forms/CancelForm.tsx
  - src/features/contracts/forms/forms.test.tsx
  - src/features/contracts/schemas.ts
  - src/features/contracts/schemas.test.ts
  - src/routes/_authenticated/contrats/$contractId.tsx
autonomous: true
requirements: [RENT-02, RENT-03, RENT-04]
must_haves:
  truths:
    - "The detail card shows vehicle, customer, period, status, deposit, and departure/return mileage+fuel, resolving plate/customer via a separate vehicle+customer fetch (D-03)"
    - "Activate shows only when status==reserved; Close only when status==active; Cancel only when status in {reserved,active} — mirroring the domain transition matrix (D-03)"
    - "Action buttons are additionally gated by canOperate(scope, vehicle.agency_id) — the agency axis, not hasOrgRole (D-09)"
    - "Activate sends mileage+fuel; Close sends return mileage+fuel + at least one invoice line (DZD→cents, integer vat_rate); Cancel requires a non-empty reason (RENT-02/03/04)"
    - "A lifecycle 409 (illegal transition on stale UI) shows its distinct message and refetches the contract to re-gate the buttons"
  artifacts:
    - src/features/contracts/ContractDetail.tsx
    - src/features/contracts/forms/ActivateForm.tsx
    - src/features/contracts/forms/CloseForm.tsx
    - src/features/contracts/forms/CancelForm.tsx
  key_links:
    - "ContractDetail composes useContractQuery + useVehicleQuery (plate + agency gate) + useCustomerQuery (name)"
    - "Forms call useActivate/useClose/useCancel from 04-01 and map 409 via transitionErrorKey, then invalidate to re-gate"
    - "$contractId route filled here (replaces the 04-02 stub)"
---

<objective>
Ship the contract detail screen at `/contrats/$contractId` (D-03): a full contract card plus the three status-gated lifecycle actions — Activate (departure mileage + fuel, RENT-02), Close (return mileage + fuel + invoice lines, RENT-03), Cancel (reason, RENT-04). Because contractResponse carries only vehicle_id/customer_id UUIDs and NO agency_id, the screen fetches the vehicle (for plate + the `canOperate` agency gate) and the customer (for the name) alongside the contract. Button visibility mirrors the backend transition matrix exactly, and a stale-UI 409 shows its distinct message and refetches to re-gate.

Purpose: this is where a contract visibly moves reserved→active→closed and where cancellation records a reason (success criteria 2 and 3).
Output: ContractDetail, the three lifecycle forms, the zod schemas for their payloads, and the filled `$contractId` route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contrats-de-location/04-RESEARCH.md
@.planning/phases/04-contrats-de-location/04-01-SUMMARY.md

# Patterns to mirror verbatim:
@src/features/fleet/VehicleDetail.tsx
@src/features/customers/CustomerCreateForm.tsx
@src/features/customers/schemas.ts
@src/shared/auth/permissions.ts
@src/features/contracts/mutations.ts
@src/features/contracts/queries.ts
@src/routes/_authenticated/contrats/$contractId.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schemas.ts + lifecycle forms (Activate / Close / Cancel) with 409 re-gating</name>
  <files>src/features/contracts/schemas.ts, src/features/contracts/schemas.test.ts, src/features/contracts/forms/ActivateForm.tsx, src/features/contracts/forms/CloseForm.tsx, src/features/contracts/forms/CancelForm.tsx, src/features/contracts/forms/forms.test.tsx</files>
  <behavior>
    - activateSchema requires mileage (coerced int, gte 0) and fuel (enum empty|quarter|half|three_quarters|full); actual_at optional.
    - closeSchema requires mileage+fuel and an invoice_lines array with min 1 row; each row: description non-empty, quantity int gt 0, a DZD amount that converts to unit_price_ht_cents via *100 (integer cents), vat_rate integer percent gte 0. Missing/zero lines fails validation with contracts.errors.invoiceLineRequired.
    - cancelSchema requires a non-empty trimmed reason (contracts.errors.reasonRequired).
    - On submit, ActivateForm/CloseForm/CancelForm call the matching mutation; a 409 sets the form's error to transitionErrorKey(kind, error) and triggers a contract refetch (invalidate ['contracts','detail',id]); a non-409 failure shows the generic per-action error key.
  </behavior>
  <action>
Create `src/features/contracts/schemas.ts` (zod, messages as i18n KEYS resolved via t() in FieldError exactly like customers/schemas.ts — never bare strings): `activateSchema` { actual_at?: string; mileage: z.coerce.number().int().gte(0); fuel: FuelLevel enum }; `closeSchema` { actual_at?; mileage; fuel; invoice_lines: z.array(lineSchema).min(1, contracts.errors.invoiceLineRequired) } where `lineSchema` = { description: min(1); quantity: coerce int gt 0; amount_dzd: coerce number gte 0; vat_rate: coerce int gte 0 }; `cancelSchema` { reason: string min(1, contracts.errors.reasonRequired) after trim }. Add a `toCloseBody(values)` mapper that converts each line's `amount_dzd` → `unit_price_ht_cents = Math.round(amount_dzd * 100)` (Pitfall 5: DZD→cents, integer; vat_rate is an integer percent, sent as-is) and assembles `CloseBody`. Write `schemas.test.ts` covering min=1 lines, DZD→cents conversion, gt-0 quantity, and reason-required.

Create the three forms under `src/features/contracts/forms/` using react-hook-form + zodResolver + the shared Field/Input/Select primitives (mirror CustomerCreateForm.tsx's Field/FieldError/translatedError idiom):
- `ActivateForm({ contract, onDone })`: mileage Input (numeric) + fuel Select (5 levels, labels reuse `vehicles.fuelLevel.*`). Submits ActivateBody via `useActivate(contract.id)`.
- `CloseForm({ contract, onDone })`: mileage + fuel + a `useFieldArray({ name: "invoice_lines" })` with a required first row (description, quantity, DZD amount, vat_rate), add/remove rows keyed by field.id (never index). Submits via `useClose(contract.id)` using toCloseBody. Amounts entered in DZD, labeled as DZD (D-10).
- `CancelForm({ contract, onDone })`: a reason textarea/Input, submits CancelBody via `useCancel(contract.id)`. (Naming per D-03; a Sheet is optional — inline is fine, no dialog primitive is vendored.)
Each form's catch: if `transitionErrorKey(kind, error)` is non-null (a 409), set an inline error with that key AND refetch the contract so the parent re-gates its buttons to the true current status (Pitfall 2 / D-04 second half); otherwise show contracts.errors.{activate,close,cancel}Failed. On success call `onDone()` (the parent invalidation already happened in the mutation). Write `forms.test.tsx` (MSW): activate happy path sends mileage+fuel; close rejects zero lines then succeeds with one line and sends integer cents; cancel requires reason; a mocked 409 on activate surfaces contracts.errors.notReservable and triggers a refetch.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/features/contracts/schemas.test.ts src/features/contracts/forms/forms.test.tsx</automated>
  </verify>
  <done>Schemas enforce the backend rules (min=1 lines, DZD→integer-cents, gt-0 quantity, required reason); the three forms submit the correct bodies and map a 409 to the distinct transition message with a refetch; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: ContractDetail screen — resolved card + status/agency-gated actions + filled route</name>
  <files>src/features/contracts/ContractDetail.tsx, src/features/contracts/ContractDetail.test.tsx, src/routes/_authenticated/contrats/$contractId.tsx</files>
  <action>
Create `src/features/contracts/ContractDetail({ contractId })` mirroring VehicleDetail.tsx's parallel-query, independent-error shape. Compose IN PARALLEL: `useContractQuery(contractId)`, then `useVehicleQuery(contract.vehicle_id)` (for plate + agency gate) and `useCustomerQuery(contract.customer_id)` (for name) — these two depend on the contract's ids, so gate them on the loaded contract (enabled once contract data exists) rather than a blind waterfall; a vehicle/customer error stays inline and never blanks the loaded contract card (D-03). Render a full contract card (D-03): vehicle (plate + brand/model from the vehicle), customer name (company legal_name else full_name), period (locale-formatted dates), status via ContractStatusBadge, deposit (deposit_amount_cents formatted as DZD when present), and departure/return mileage+fuel (all omitempty — presence-guard each, mirror VehicleDetail's guards). A 404 on the contract renders one generic not-found state (contracts.detail.notFound*), never an access-denied variant (mirror VehicleDetail T-02-06).

Gate the lifecycle action buttons by BOTH the transition matrix AND the agency axis (D-03 + D-09): compute `mayOperate = scope !== null && vehicle !== undefined && canOperate(scope, vehicle.agency_id)` — this uses `canOperate` with the VEHICLE'S agency_id (04-RESEARCH.md Authorization: contracts are agency-scoped via the vehicle, NOT hasOrgRole; and agency_id is read off the vehicle, never off the contract). Then show Activate only when `contract.status === "reserved" && mayOperate`; Close only when `contract.status === "active" && mayOperate`; Cancel only when `contract.status ∈ {reserved, active} && mayOperate`. Render the matching form (Task 1) inline/behind each button; each form's `onDone` closes it and the mutation's invalidation refetches the detail so the buttons re-gate to the new status. Expose a record-deposit action too (optional, per D-03 detail exposes deposit) if the status allows — keep it minimal (reuse useRecordDeposit). A back link → /contrats (contracts.actions.backToList).

Fill `src/routes/_authenticated/contrats/$contractId.tsx`: replace the 04-02 EmptyState stub with the real route that reads `Route.useParams().contractId` and renders `<ContractDetail contractId={contractId} />` (mirror clients/$customerId.tsx exactly).

Write `ContractDetail.test.tsx` (MSW + the 04-01 fixtures, and a scope fixture from src/test/fixtures/scope.ts): a reserved contract shows Activate + Cancel and NOT Close; an active contract shows Close + Cancel and NOT Activate; a closed/cancelled contract shows none; a scope WITHOUT canOperate on the vehicle's agency hides all action buttons even for a reserved contract; the card resolves plate + customer name (not raw UUIDs); a 404 renders the generic not-found state.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/features/contracts/ContractDetail.test.tsx</automated>
  </verify>
  <done>Detail resolves plate+customer via separate fetches; buttons appear exactly per the transition matrix AND canOperate(vehicle.agency_id); actions succeed and re-gate on success; a stale 409 re-gates; 404 is generic; tsc clean; `npx vitest run src/features/contracts` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → wheelio-api REST | Lifecycle actions cross here; button gating is UX only, backend re-enforces canOperate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | action-button gate | high | mitigate | Buttons gated on canOperate(scope, vehicle.agency_id) — the correct agency axis; backend re-enforces every mutation with its own 403 |
| T-04-05 | Tampering | stale-UI illegal transition | medium | mitigate | A lifecycle 409 shows its distinct transition message and refetches the contract to re-gate |
| T-04-07 | Information Disclosure | contract 404 (existence oracle) | low | mitigate | One generic not-found state for both nonexistent and out-of-scope contracts (no access-denied variant) |
| T-04-04 | Information Disclosure | form error text | low | mitigate | Errors map to i18n keys, never raw problem detail |
| T-04-SC | Tampering | npm installs | high | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npx vitest run src/features/contracts` green.
- Button visibility matches the transition matrix AND the agency gate under test.
- agency_id is read only from the vehicle, never from the contract.
</verification>

<success_criteria>
A user can open a contract, see it fully resolved (plate, customer, period, status, deposit, mileage/fuel), and — when authorized on the vehicle's agency — activate a reservation, close an active contract with invoice lines, or cancel with a reason, watching the status and available actions update; stale-UI conflicts surface a clear distinct message and self-heal via refetch.
</success_criteria>

<output>
Create `.planning/phases/04-contrats-de-location/04-03-SUMMARY.md` when done.
</output>
