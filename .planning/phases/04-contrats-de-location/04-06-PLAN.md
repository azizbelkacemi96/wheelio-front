---
phase: 04-contrats-de-location
plan: 06
wave: 5
depends_on: ["04-02", "04-04", "04-05"]
autonomous: true
requirements: [OPS-01, RENT-05]
files_modified:
  - src/features/contracts/OpsToday.tsx
  - src/features/contracts/OpsToday.test.tsx
  - src/routes/_authenticated/index.tsx
  - src/shared/i18n/fr/common.json
  - src/shared/i18n/en/common.json
  - src/routes/_authenticated/placeholders.test.tsx
  - e2e/auth.spec.ts
  - e2e/contract-wizard.spec.ts
must_haves:
  - "The `/` landing shows today's PICKUPS (reserved contracts starting today) and today's RETURNS (active contracts ending today) by consuming 04-01's already-shipped useTodayOverviewQuery (Africa/Algiers composition) — NO second today-composition implementation; each row resolves vehicle plate + customer name (04-01 resolve.ts) and links to /contrats/$contractId"
  - "The Africa/Algiers boundary correctness is owned + tested by 04-01 (dateAlgiers + useTodayOverviewQuery); 04-06 reuses it and does not re-derive the date math"
  - "Empty state (04-01 ops.emptyAll) when neither section has rows; combined loading + error states from the useTodayOverviewQuery result"
  - "All prior E2E stays green: `/` and `/contrats` are no longer placeholders — placeholder assertions migrate to /etats-des-lieux (the last placeholder), and the E2E mock serves per-vehicle contract responses"
  - "A wizard E2E happy path drives /contrats/nouveau end to end (vehicle→customer→terms→departure→finish) and lands on a contract detail"
  - "Full phase gate green: tsc + build + vitest + playwright"
---

<objective>
Deliver OPS-01 (the today overview on the `/` landing) and close Phase 4: replace the placeholder landing with the composed dashboard, migrate the E2E/placeholder assertions off `/` and `/contrats`, add the wizard happy-path E2E, and run the full phase gate.

Output: `OpsToday` screen + `todayQueries.ts` composition (useQueries fan-out + Africa/Algiers filtering, reusing 04-01's dateAlgiers helper), the filled `/` route, i18n, and the E2E updates.
</objective>

<context>
@.planning/phases/04-contrats-de-location/04-RESEARCH.md  (OPS-01 Today Overview section + timezone helper)
@.planning/phases/04-contrats-de-location/04-01-PLAN.md   (dateAlgiers + resolve helpers)
@src/features/fleet/queries.ts
@src/features/contracts/queries.ts
@e2e/auth.spec.ts
</context>

<task>
  <name>Task 1: OpsToday screen (consumes 04-01 useTodayOverviewQuery) + `/` route + i18n</name>
  <files>src/features/contracts/OpsToday.tsx, src/features/contracts/OpsToday.test.tsx, src/routes/_authenticated/index.tsx, src/shared/i18n/fr/common.json, src/shared/i18n/en/common.json</files>
  <behavior>
    - Consumes 04-01's `useTodayOverviewQuery` (already does the useQueries fan-out + Africa/Algiers filtering + pickup/return split). 04-06 does NOT re-implement the composition or the date math.
    - Two sections (ops.pickups / ops.returns titles from 04-01's namespace), each a compact list of rows (plate — customer — time) linking to /contrats/$contractId.
    - Combined loading skeleton; error banner with retry; both-empty → 04-01's ops.emptyAll ("Rien de prévu aujourd'hui").
    - Replaces the EmptyState at `/` (src/routes/_authenticated/index.tsx now renders OpsToday).
  </behavior>
  <action>
    Build OpsToday consuming `useTodayOverviewQuery` from src/features/contracts/queries.ts. REUSE the `ops.*` keys 04-01 already shipped (ops.title, ops.pickups, ops.returns, ops.emptyAll); add ONLY any genuinely-missing row-level keys (e.g. ops.loadError / ops.retry / a time-label) after reading the current fr/en common.json — do not mint parallel names for keys 04-01 already defined, and keep FR/EN parity. Fill the `/` route. RED first: OpsToday.test.tsx — populated pickups+returns (with resolved plate/customer + $contractId links), both-empty state, error state. The boundary/date-math test already lives with 04-01's dateAlgiers/useTodayOverviewQuery — do not duplicate it here.
  </action>
  <verify>
    <automated>cd /Users/azizbelkacemi/Desktop/work-dev/wheelio-front && npx vitest run src/features/contracts/OpsToday.test.tsx src/shared/i18n</automated>
  </verify>
  <done>OpsToday renders both sections with resolved rows + links, both-empty + error states; `/` shows it; ops.* keys reused (no parallel set); FR/EN parity; tsc clean.</done>
</task>

<task>
  <name>Task 2: E2E/placeholder migration + wizard happy-path E2E + phase gate</name>
  <files>src/routes/_authenticated/placeholders.test.tsx, e2e/auth.spec.ts, e2e/contract-wizard.spec.ts</files>
  <behavior>
    - `/` and `/contrats` are no longer placeholders → drop them from placeholders.test.tsx; migrate any remaining "Bientôt disponible" navigation assertion in auth.spec.ts to /etats-des-lieux (the last placeholder). The `/` landing E2E now expects the OpsToday dashboard (mock per-vehicle contract responses so it renders deterministically — e.g. empty "Rien de prévu aujourd'hui" is acceptable and simplest).
    - New e2e/contract-wizard.spec.ts: log in (owner), go to /contrats/nouveau, complete step1 vehicle → step2 pick existing customer → step3 dates(+deposit optional) → step4 mileage+fuel → finish, assert landing on a /contrats/{id} detail. Mock create(201)+activate(200) and the vehicles/customers the steps read.
  </behavior>
  <action>
    Update the E2E mock (auth.spec.ts mockApi) to serve GET /vehicles/:id/rental-contracts (empty or fixture) so the landing + list don't 404. Add the wizard spec with its own mocks. Keep CORS + /v1 path-strip handling already in the mock.
    Run the FULL phase gate and report exact numbers.
  </action>
  <verify>
    <automated>cd /Users/azizbelkacemi/Desktop/work-dev/wheelio-front && npx tsc --noEmit && npm run build && npx vitest run && npx playwright test</automated>
  </verify>
  <done>All unit tests green; both E2E specs (auth + contract-wizard) pass; tsc + build clean; Phase 4 gate complete.</done>
</task>
