---
phase: 04-contrats-de-location
plan: 05
wave: 4
depends_on: ["04-01", "04-04"]
autonomous: true
requirements: [RENT-05, RENT-01, RENT-02]
files_modified:
  - src/features/contracts/wizard/useContractWizardFinish.ts
  - src/features/contracts/wizard/useContractWizardFinish.test.tsx
  - src/features/contracts/wizard/ContractWizard.tsx
  - src/shared/i18n/fr/common.json
  - src/shared/i18n/en/common.json
must_haves:
  - "Finish runs create → (optional deposit) → (optional activate) as an ORDERED sequence; an already-succeeded create is NEVER re-POSTed on retry (partial-failure invariant, same discipline as customers/mutations.ts create-then-attach)"
  - "A create-time 409 surfaces the friendly contracts.errors.overlap message and does NOT advance/redirect"
  - "On full success the user lands on /contrats/$contractId of the newly created (and, if activate_now, activated) contract"
  - "On create success but deposit/activate failure, the contract still exists (reserved) — surface the partial state, offer retry of ONLY the failed remaining step, and route to the detail screen so the user can finish there"
  - "tsc + build + vitest green"
---

<objective>
Fill the finish SEAM that 04-04 left open on the ContractWizard: turn the wizard's collected RHF values into the real backend sequence create → optional deposit → optional activate, with the exact partial-failure discipline established in Phase 3's create-then-attach (customers/mutations.ts). This is the last piece of RENT-05's "one continuous flow" and it is where RENT-01 (overlap rejection) and RENT-02 (activation) are actually exercised from the wizard.

Output: `src/features/contracts/wizard/useContractWizardFinish.ts` (+ test), the ContractWizard shell wired to call it on the last step, and the finish-related i18n copy.
</objective>

<context>
@.planning/phases/04-contrats-de-location/04-RESEARCH.md
@.planning/phases/04-contrats-de-location/04-04-PLAN.md
@src/features/customers/mutations.ts
@src/features/contracts/mutations.ts
@src/features/contracts/wizard/schema.ts
</context>

<task>
  <name>Task 1: useContractWizardFinish — ordered create→deposit?→activate with partial-failure</name>
  <files>src/features/contracts/wizard/useContractWizardFinish.ts, src/features/contracts/wizard/useContractWizardFinish.test.tsx, src/shared/i18n/fr/common.json, src/shared/i18n/en/common.json</files>
  <behavior>
    - finish(values): step A POST create `{customer_id, starts_at, ends_at}` on the chosen vehicle → 201 reserved contract. A 409 here → reject with contracts.errors.overlap; do not proceed.
    - step B (only if values.deposit_amount is set): POST /rental-contracts/{id}/deposit `{amount_cents, method}`. Convert the DZD amount to cents.
    - step C (only if values.activate_now): POST /rental-contracts/{id}/activate `{mileage, fuel}` (always send mileage even when 0; fuel is required).
    - PARTIAL-FAILURE INVARIANT: the created contract id is held in a ref/state; if B or C fails, a retry MUST NOT re-run step A (no duplicate contract, no second overlap check). Retry resumes at the first not-yet-succeeded step. Mirror customers/mutations.ts create-then-attach exactly.
    - Returns/exposes: `{ finish, isPending, createdContractId, failedStep }` so the shell can route to the detail screen and show which step to retry.
  </behavior>
  <action>
    Build `useContractWizardFinish.ts` as a hook composing the EXACT 04-01 mutation exports: `useCreateContract`, `useRecordDeposit` (deposit), and `useActivate` — do NOT hand-roll a duplicate deposit/activate call. Hold `createdContractId` in a ref so a re-invoked finish skips step A when it is already set. For the create-409→overlap mapping, reuse 04-01's `overlapErrorKey` helper rather than re-deriving the message. Map amount DZD→cents (integer, no float drift — multiply then round). Map a create 409 (HTTPError, problem+json) to contracts.errors.overlap; rethrow other errors with their real reason (no swallowing). Add i18n keys: contracts.errors.overlap (if not already from 04-01), wizard.finish.depositFailed, wizard.finish.activateFailed, wizard.finish.retry, wizard.finish.partialNote — FR + EN, parity.
    RED first: useContractWizardFinish.test.tsx with MSW — (1) full happy path create+deposit+activate; (2) create 409 → overlap message, no deposit/activate calls; (3) create OK + activate 500 → createdContractId set, failedStep="activate", and a second finish() call does NOT re-POST create (assert create called exactly once); (4) activate_now=false → no activate call.
  </action>
  <verify>
    <automated>cd /Users/azizbelkacemi/Desktop/work-dev/wheelio-front && npx vitest run src/features/contracts/wizard/useContractWizardFinish.test.tsx</automated>
  </verify>
  <done>All four finish scenarios pass; create is provably called once across a retry; tsc clean.</done>
</task>

<task>
  <name>Task 2: Wire the seam into ContractWizard + route to detail on success</name>
  <files>src/features/contracts/wizard/ContractWizard.tsx, src/features/contracts/wizard/useContractWizardFinish.test.tsx</files>
  <behavior>
    - The wizard's last-step primary button calls finish(getValues()); while isPending it shows a spinner/disabled state.
    - On full success: navigate to /contrats/{createdContractId}.
    - On partial failure (createdContractId set, failedStep !== null): show wizard.finish.partialNote + a retry button for the failed step; also offer a link to /contrats/{createdContractId} so the user can complete the remaining action on the detail screen (04-03 forms).
    - A create-time overlap shows the overlap message inline on the terms step (or a toast) and stays in the wizard so the user can pick different dates/vehicle.
  </behavior>
  <action>
    Replace the 04-04 onFinish seam callback with useContractWizardFinish. Keep the wizard's own RHF/step-index intact. Use the app's navigate() (TanStack Router) for the success redirect. Do not persist wizard state across a full reload (fresh wizard each mount — D-05a).
  </action>
  <verify>
    <automated>cd /Users/azizbelkacemi/Desktop/work-dev/wheelio-front && npx vitest run src/features/contracts/wizard && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Wizard finish invokes the sequence; success routes to the new contract's detail; partial failure surfaces retry + a detail link; overlap keeps the user in the wizard; tsc + build green.</done>
</task>
