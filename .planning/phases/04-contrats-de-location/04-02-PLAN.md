---
phase: 04-contrats-de-location
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/features/contracts/ContractStatusBadge.tsx
  - src/features/contracts/ContractStatusBadge.test.tsx
  - src/features/contracts/ContractList.tsx
  - src/features/contracts/ContractList.test.tsx
  - src/routes/_authenticated/contrats/index.tsx
  - src/routes/_authenticated/contrats/$contractId.tsx
  - src/routes/_authenticated/contrats/nouveau.tsx
  - src/routes/_authenticated/contrats.tsx
  - src/routes/_authenticated/placeholders.test.tsx
autonomous: true
requirements: [RENT-01, RENT-02, RENT-03, RENT-04]
must_haves:
  truths:
    - "The /contrats list is composed client-side (fan-out over vehicles + per-vehicle rental-contracts), never a single list-all fetch"
    - "Each row shows vehicle plate + customer name + period + status, with plate/customer resolved via the id→name join (contractResponse carries only UUIDs)"
    - "A status filter (all/reserved/active/closed/cancelled) and a text search narrow the composed list"
    - "The status badge label always comes from contracts.status.* i18n; raw enum strings never reach the DOM (D-01)"
    - "The 'New contract' CTA is gated on hasOrgRole(scope,'agent') and links to /contrats/nouveau"
  artifacts:
    - src/features/contracts/ContractStatusBadge.tsx
    - src/features/contracts/ContractList.tsx
    - src/routes/_authenticated/contrats/index.tsx
  key_links:
    - "ContractList consumes useAllContractsQuery + resolve.byId/toContractView from 04-01"
    - "Row Link → /contrats/$contractId (stub created here, filled by 04-03); CTA Link → /contrats/nouveau (stub created here, filled by 04-04)"
    - "placeholders.test.tsx drops /contrats the moment the route becomes real (mirrors 03-02's /clients migration)"
---

<objective>
Ship the contracts list at `/contrats` (D-02): a dense responsive table (md+) / card stack (below md) — the same shape as VehicleList/CustomerList — driven by the client-side composition from 04-01, with a status filter, a text search, the reusable ContractStatusBadge, and a role-gated "New contract" CTA. Convert the flat `contrats.tsx` placeholder into a directory route and pull forward the `$contractId` and `nouveau` stub routes as typed Link targets (the established 02-02/03-02 precedent), and migrate the placeholder-route test so /contrats stops asserting the empty state.

Purpose: the list is the visibility surface where a reservation appears and where a contract is seen "visibly moving" reserved→active→closed (success criterion 2). It is also the entry point to detail (04-03) and the wizard (04-04).
Output: ContractStatusBadge, ContractList, the `contrats/` directory route (index real + two typed stubs), and an updated placeholders.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contrats-de-location/04-RESEARCH.md
@.planning/phases/04-contrats-de-location/04-01-SUMMARY.md

# Patterns to mirror verbatim:
@src/features/customers/CustomerList.tsx
@src/features/fleet/StatusBadge.tsx
@src/features/fleet/VehicleList.tsx
@src/routes/_authenticated/clients/index.tsx
@src/routes/_authenticated/contrats.tsx
@src/routes/_authenticated/placeholders.test.tsx
@src/features/contracts/queries.ts
@src/features/contracts/resolve.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContractStatusBadge + route conversion (index real, $contractId + nouveau stubs, placeholder-test drop)</name>
  <files>src/features/contracts/ContractStatusBadge.tsx, src/features/contracts/ContractStatusBadge.test.tsx, src/routes/_authenticated/contrats/index.tsx, src/routes/_authenticated/contrats/$contractId.tsx, src/routes/_authenticated/contrats/nouveau.tsx, src/routes/_authenticated/contrats.tsx, src/routes/_authenticated/placeholders.test.tsx</files>
  <action>
Create `src/features/contracts/ContractStatusBadge.tsx` mirroring fleet/StatusBadge.tsx exactly (D-01): a `Record<ContractStatus, string>` style map (so a new backend status fails tsc — the DTO drift idiom) and the label ALWAYS from `t("contracts.status.${status}")`. Suggested token styling: reserved → primary tint, active → warning or primary, closed → success tint, cancelled → muted/border. Add `ContractStatusBadge.test.tsx` asserting each status renders its FR label and switches to EN on locale change.

Convert the flat placeholder `src/routes/_authenticated/contrats.tsx` into a directory route mirroring `clients/`: DELETE `contrats.tsx` and create `contrats/index.tsx` whose component is `ContractList` (created in Task 2) — same shape as clients/index.tsx (no loader; component-level useQuery is the idiom). Create `contrats/$contractId.tsx` as a typed EmptyState STUB (like the 02-02 `$vehicleId`/03-02 `$customerId` pull-forward precedent recorded in STATE.md) — it renders EmptyState for now and is filled by 04-03; it must register the route so ContractList's row Link type-checks under `tsc -b`. Create `contrats/nouveau.tsx` as a typed EmptyState STUB filled by 04-04, so the "New contract" CTA Link type-checks. Keep routeTree.gen.ts gitignored (regenerated by the predev/prebuild/pretest hooks).

Migrate `src/routes/_authenticated/placeholders.test.tsx`: remove the `import { Route as ContratsRoute } from "./contrats"` line and the `"/contrats": ContratsRoute` entry from `allPlaceholderRoutes` (exactly as the file already dropped /vehicules and /clients — add a matching NOTE comment: "/contrats is no longer a placeholder — plan 04-02 replaced it with the real ContractList screen"). Leave `/`, `/etats-des-lieux`, and the admin routes as the remaining placeholder set; `/etats-des-lieux` stays the file's positive-assertion anchor. Do NOT touch the `/` (index) entry — that migrates in 04-06 when OpsToday goes live.
  </action>
  <verify>
    <automated>npm run generate-routes && npx tsc --noEmit && npx vitest run src/features/contracts/ContractStatusBadge.test.tsx src/routes/_authenticated/placeholders.test.tsx</automated>
  </verify>
  <done>ContractStatusBadge green FR+EN; contrats is a directory route (index real, $contractId + nouveau typed stubs); placeholders.test.tsx no longer imports/asserts /contrats and stays green; tsc -b resolves all contrats/* Link targets.</done>
</task>

<task type="auto">
  <name>Task 2: ContractList screen — composed list, id→name join, status filter + search, gated CTA</name>
  <files>src/features/contracts/ContractList.tsx, src/features/contracts/ContractList.test.tsx</files>
  <action>
Create `src/features/contracts/ContractList.tsx` (D-02) mirroring CustomerList.tsx's responsive shape (dense `<Table>` on md+, `<Card>` stack below md, both always in the DOM because jsdom can't evaluate media queries; every visible string via i18n, only raw data values bare). Data comes from `useAllContractsQuery()` (04-01) — the client-side composition (fan-out over vehicles + per-vehicle rental-contracts); this plan MUST NOT call a list-all endpoint (it does not exist — 04-RESEARCH.md reality #1). Build the id→name join in-memory with `resolve.byId` over the vehicles returned by the composition and over a customers list (fetch once via `useCustomersQuery("")` from features/customers/queries — batch join, research Open Question 3) and render each row via `toContractView`: columns are vehicle plate, customer name, period (starts_at → ends_at, locale-formatted via useLocale/toLocaleDateString), and the ContractStatusBadge (columns exactly per D-02). When a vehicle/customer can't be resolved, render an em dash — never crash and never read agency_id off the contract.

Add a status filter (a `Select` with options all/reserved/active/closed/cancelled, using `contracts.filter.*` labels, "all" via the ALL sentinel pattern from VehicleList) applied client-side over the composed array, and a text search (client-side useMemo over plate + resolved customer name — the composed set is already fully in memory, so a debounced server search is neither possible nor needed here; contrast this with CustomerList's server ?q=). Loading → skeleton; error (any child query error) → inline retry banner using contracts.loadError/contracts.retry; genuinely empty (no contracts, no active filter/search) → EmptyState with contracts.empty.*; zero-after-filter → contracts.noResults. Header exposes a "New contract" CTA `Button asChild` → `Link to="/contrats/nouveau"`, gated `scope !== null && hasOrgRole(scope, "agent")` (04-RESEARCH.md Authorization: the /contrats CTA has no agency context yet, so it uses the org-wide "agent in SOME agency" gate, then the wizard narrows per-vehicle — mirrors CustomerList's CTA idiom; the backend re-enforces). Rows Link to `/contrats/$contractId` with `params={{ contractId: c.id }}`.

Wire the route: set `contrats/index.tsx`'s component to `ContractList`. Write `ContractList.test.tsx` (MSW-backed, using the 04-01 fixtures spanning all four statuses) asserting: the composed list renders plate + customer name + period + a status badge for at least one contract of each status; the status filter narrows to a single status; the text search matches by plate and by customer name; the CTA is present for an agent-capable scope and absent otherwise; an empty composition shows the empty state.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/features/contracts/ContractList.test.tsx</automated>
  </verify>
  <done>The list is fully composed client-side (no list-all call), shows plate/customer/period/status with the id→name join, status filter + search work, the CTA is correctly role-gated, and all UI states (loading/error/empty/no-results/populated) are covered; tsc clean; `npx vitest run src/features/contracts` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → wheelio-api REST | The list reads per-vehicle contracts; the CTA gate is UX-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Elevation of Privilege | "New contract" CTA gate | medium | mitigate | CTA gated on hasOrgRole(scope,'agent') (UX only); the wizard narrows per-vehicle and the backend re-enforces create authz with its own 403 |
| T-04-04 | Information Disclosure | list error banner | low | mitigate | Composition errors show contracts.loadError copy, never raw problem detail |
| T-04-SC | Tampering | npm installs | high | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npx vitest run src/features/contracts` green.
- placeholders.test.tsx green with /contrats removed and /etats-des-lieux still asserted.
- The list makes zero calls to any nonexistent list-all endpoint (composition only).
</verification>

<success_criteria>
A user visiting /contrats sees every contract across all statuses with plate, customer, period, and a status badge, can filter by status and search by plate/customer, and (if agent+) sees a working "New contract" CTA — all composed client-side, with the placeholder route retired and the test suite green.
</success_criteria>

<output>
Create `.planning/phases/04-contrats-de-location/04-02-SUMMARY.md` when done.
</output>
