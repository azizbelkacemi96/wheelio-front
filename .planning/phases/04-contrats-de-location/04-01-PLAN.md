---
phase: 04-contrats-de-location
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/rental.ts
  - src/features/contracts/api.ts
  - src/features/contracts/queries.ts
  - src/features/contracts/queries.test.tsx
  - src/features/contracts/mutations.ts
  - src/features/contracts/mutations.test.tsx
  - src/features/contracts/dateAlgiers.ts
  - src/features/contracts/dateAlgiers.test.ts
  - src/features/contracts/resolve.ts
  - src/features/contracts/resolve.test.ts
  - src/test/fixtures/contracts.ts
  - src/test/mocks/handlers.ts
  - src/test/mocks/handlers.test.ts
  - src/shared/i18n/fr/common.json
  - src/shared/i18n/en/common.json
autonomous: true
requirements: [RENT-01, RENT-02, RENT-03, RENT-04, OPS-01]
must_haves:
  truths:
    - "createContract/activate/close/cancel/recordDeposit each POST the exact wheelio-api route and body shape, ids percent-encoded"
    - "A create-time 409 maps to the friendly overlap key; an activate/close/cancel 409 maps to its distinct illegal-transition key (D-04)"
    - "Lifecycle mutations invalidate BOTH ['contracts'] and ['vehicles'] on success (D-06)"
    - "The contracts list AND today overview are composed client-side by fanning out over vehicles then per-vehicle rental-contracts (no list-all endpoint)"
    - "'Today' is computed in Africa/Algiers day-key, correct at a near-midnight UTC boundary"
    - "contracts.* / wizard.* / ops.* copy exists in FR and EN with identical key sets; reuses contracts.status.* already shipped"
  artifacts:
    - src/types/rental.ts
    - src/features/contracts/api.ts
    - src/features/contracts/queries.ts
    - src/features/contracts/mutations.ts
    - src/features/contracts/dateAlgiers.ts
    - src/features/contracts/resolve.ts
    - src/test/fixtures/contracts.ts
  key_links:
    - "queries.ts fan-out (useContractsForVehicles) consumes fleet's useVehiclesQuery + api.listContractsByVehicle"
    - "mutations.ts 409 mapping consumed by the wizard finish (04-05) and detail action forms (04-03)"
    - "resolve.ts id→plate/customer/agency lookups consumed by list (04-02), detail (04-03), ops (04-06)"
---

<objective>
Build the entire client-side data/contract foundation for Phase 4 rentals: verbatim request DTOs, the thin `api.ts` calls, the TanStack Query read layer (including the client-side composition that replaces the non-existent list-all endpoint), the lifecycle mutations with their 409 disambiguation and dual-namespace invalidation, the Africa/Algiers date helper, the UUID→name/plate/agency resolution utility, and the MSW handlers + fixtures + FR/EN i18n every downstream screen composes from.

This plan mirrors the shape of the shipped 03-01 customer foundation and 02-01 fleet foundation (D-11): `src/features/contracts/` gets its own api/queries/mutations, exactly as `features/fleet` and `features/customers` do.

Purpose: after this plan, plans 04-02..04-06 are pure composition — no further contract/API/permission/i18n groundwork is needed.
Output: `src/features/contracts/{api,queries,mutations,dateAlgiers,resolve}.ts` (+ tests), extended `src/types/rental.ts`, new `src/test/fixtures/contracts.ts`, extended MSW handlers, and the `contracts.*`/`wizard.*`/`ops.*` FR/EN i18n namespaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contrats-de-location/04-RESEARCH.md
@.planning/phases/04-contrats-de-location/04-CONTEXT.md

# Patterns to mirror verbatim (read before writing):
@src/types/rental.ts
@src/features/fleet/api.ts
@src/features/fleet/queries.ts
@src/features/customers/api.ts
@src/features/customers/queries.ts
@src/features/customers/mutations.ts
@src/features/customers/mutations.test.tsx
@src/test/mocks/handlers.ts
@src/test/fixtures/fleet.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rental request DTOs + MSW lifecycle handlers + fixtures + contracts/wizard/ops i18n</name>
  <files>src/types/rental.ts, src/test/fixtures/contracts.ts, src/test/mocks/handlers.ts, src/test/mocks/handlers.test.ts, src/shared/i18n/fr/common.json, src/shared/i18n/en/common.json</files>
  <action>
Extend `src/types/rental.ts` (do NOT rewrite the existing ContractResponse/ContractStatus/FuelLevel — append) with request DTOs mirroring rental_dto.go verbatim per 04-RESEARCH.md "API Contract" (D-01, keep the file's "read-only mirror of Go" doc discipline): `CreateContractBody` { customer_id: string; starts_at: string; ends_at: string } — note NO deposit, NO agency_id in the create body (contract is always born reserved); `ActivateBody` { actual_at?: string; mileage: number; fuel: FuelLevel }; `CloseInvoiceLine` { description: string; quantity: number; unit_price_ht_cents: number; vat_rate: number }; `CloseBody` { actual_at?: string; mileage: number; fuel: FuelLevel; invoice_lines: CloseInvoiceLine[] }; `CancelBody` { reason: string }; `DepositBody` { amount_cents: number; method: "cash" | "card" | "transfer" }. Document unit_price_ht_cents as HT (pre-VAT) in cents and vat_rate as an integer percent (e.g. 19).

Create `src/test/fixtures/contracts.ts` typed against those DTOs (so contract drift fails tsc). Import the existing vehicle fixtures from `./fleet` and customer fixtures from `./customers` so vehicle_id/customer_id reference REAL fixtures (needed later for name/plate joins). Provide: one contract in EACH status — `reservedContract`, `activeContract` (reuse/re-export the existing activeContractFixture shape from fleet.ts to avoid duplication — import it and include it in this file's exported array), `closedContract` (with return_mileage/return_fuel_level set), `cancelledContract` (with cancel_reason/cancelled_at). Provide an OVERLAP PAIR: two reserved contracts on the SAME vehicle with overlapping starts_at/ends_at windows, for the create-409 scenario. Provide a `reservedTodayContract` whose starts_at is "today" in Africa/Algiers (compute a fixed ISO by formatting `new Date()` — or hardcode a helper the test can pin) and an `activeEndingTodayContract` whose ends_at is today. Export a `contractFixtures: ContractResponse[]` aggregate and a `contractsByVehicleId` lookup map. Keep every vehicle_id/customer_id pointing at an existing fleet/customer fixture id.

Extend `src/test/mocks/handlers.ts` with a "Rentals (Phase 4)" section (the per-vehicle GET list already exists at the fleet section — do NOT duplicate it; instead make it read from the new `contractFixtures`/`contractsByVehicleId` if it currently reads fleet's single-contract array — update that one handler's data source to the new fixtures so all four statuses are listable). Add: `POST /vehicles/:vehicleID/rental-contracts` → 201 echoing a reserved ContractResponse built from the posted body (id via crypto.randomUUID, status "reserved"); a way to make it return the overlap 409 — implement it so that when the posted customer_id/window matches a fixture's overlap marker OR (simpler, deterministic) expose the 409 via a per-test `server.use(...)` override documented in the handler comment — but ALSO provide a default handler that returns 201 for the happy path. `POST /rental-contracts/:contractID/activate` → 200 echoing status "active" with departure_mileage/departure_fuel_level/actual_departure_at set from the body. `POST /rental-contracts/:contractID/close` → assert invoice_lines.length >= 1 else 400 (mirror the min=1 rule), else 200 status "closed" with return_* set. `POST /rental-contracts/:contractID/cancel` → require a non-empty reason (400 if missing) else 200 status "cancelled" with cancel_reason/cancelled_at set. `GET /rental-contracts/:contractID` → 200 from contractFixtures lookup, 404 if absent. `POST /rental-contracts/:contractID/deposit` → 200 with deposit_amount_cents/deposit_method set. Return application/problem+json shape { type, title, status, detail, instance } for the 409 overlap variant per 04-RESEARCH.md "Overlap Error Shape". Update `handlers.test.ts` smoke assertions + the handler count.

Add the FR + EN i18n under `contracts.*`, `wizard.*`, `ops.*` in BOTH `src/shared/i18n/fr/common.json` and `src/shared/i18n/en/common.json` with IDENTICAL key sets (D-10, FR/EN parity gate). REUSE the already-shipped `contracts.status.{reserved,active,closed,cancelled}` (do NOT redefine them). Add: `contracts.title`, `contracts.searchPlaceholder`, `contracts.noResults`, `contracts.loadError`, `contracts.retry`, `contracts.empty.{heading,body}`, `contracts.columns.{vehicle,customer,period,status}`, `contracts.actions.{new,activate,close,cancel,recordDeposit,backToList}`, `contracts.errors.{overlap,notReservable,notClosable,notCancellable,createFailed,activateFailed,closeFailed,cancelFailed,endBeforeStart,reasonRequired,invoiceLineRequired}`, `contracts.deposit.{amount,method,cash,card,transfer}`, `contracts.detail.{title,vehicle,customer,period,status,deposit,departureMileage,departureFuel,returnMileage,returnFuel,cancelReason,notFoundHeading,notFoundBody,notAuthorized}`, and `contracts.filter.{all,reserved,active,closed,cancelled}`. For the 5 fuel-level labels, REUSE the existing `vehicles.fuelLevel.*` keys (identical enum — do not duplicate) and document that decision inline. Add `wizard.*`: `wizard.title`, `wizard.steps.{vehicle,customer,terms,departure}`, `wizard.nav.{back,next,finish}`, `wizard.vehicle.{title,pick,noneAvailable}`, `wizard.customer.{title,pickExisting,createNew,searchPlaceholder}`, `wizard.terms.{title,startsAt,endsAt,depositOptional}`, `wizard.departure.{title,mileage,fuel,activateNow,seamNote}`, `wizard.finish.{creating,reservedNoActivate,success}`. The `wizard.departure.seamNote` FR copy names the Phase 5 handoff ("L'état des lieux photo détaillé sera disponible à l'étape suivante."). Add `ops.*`: `ops.title`, `ops.pickups.{title,empty}`, `ops.returns.{title,empty}`, `ops.emptyAll`. Amounts are DZD and dates locale-formatted downstream — keep copy currency/date-agnostic here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/test/mocks/handlers.test.ts</automated>
  </verify>
  <done>tsc clean with the new DTOs; MSW handler smoke tests green; FR and EN common.json both parse and carry identical contracts.*/wizard.*/ops.* key sets (a scripted key-count parity check over the three namespaces passes); contracts.status.* untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: api.ts + queries.ts (fan-out composition) + Africa/Algiers date helper + resolve.ts join util</name>
  <files>src/features/contracts/api.ts, src/features/contracts/queries.ts, src/features/contracts/queries.test.tsx, src/features/contracts/dateAlgiers.ts, src/features/contracts/dateAlgiers.test.ts, src/features/contracts/resolve.ts, src/features/contracts/resolve.test.ts</files>
  <behavior>
    - dateAlgiers: dayKeyAlgiers("2026-07-28T23:30:00Z") returns "2026-07-29" (UTC 23:30 is 00:30 Algiers next day) — the near-midnight boundary that naive UTC slicing gets wrong; dayKeyAlgiers and isTodayAlgiers agree for a timestamp whose Algiers day equals the injected "now"; toRFC3339Algiers("2026-07-28T09:30") returns "2026-07-28T09:30:00+01:00".
    - resolve: byId([{id:"a"},{id:"b"}]) builds a Map keyed by id; toContractView(contract, vehicleMap, customerMap) returns { contract, plate, customerName, agencyId } resolving plate from the vehicle, customerName from the customer (company legal_name else full_name), agencyId from the vehicle; missing vehicle/customer yields undefined plate/customerName/agencyId (never throws).
    - queries: useContractsForVehicles([v1,v2], "reserved") issues one listContractsByVehicle call per id in parallel and its combined data is the flattened concatenation; combined isPending is true if any child is pending, isError if any child errored. useAllContractsQuery composes useVehiclesQuery(null) then fans out with no status filter. useTodayOverviewQuery returns { pickups, returns } where pickups are reserved contracts starting today (Algiers) and returns are active contracts ending today (Algiers).
  </behavior>
  <action>
Create `src/features/contracts/api.ts` as thin ky calls over the shared `@/shared/api/client` `api` (D-11, mirror fleet/customers api.ts exactly — NO second HTTP client). Add a private `encodeIdSegment(id)` = `encodeURIComponent(id)` and use it on every id interpolated into a path (threat T-04-02, mirrors customers/api.ts). Export: `createContract(vehicleId, body: CreateContractBody): Promise<ContractResponse>` → POST `vehicles/{id}/rental-contracts`; `activateContract(contractId, body: ActivateBody)` → POST `rental-contracts/{id}/activate`; `closeContract(contractId, body: CloseBody)`; `cancelContract(contractId, body: CancelBody)`; `recordDeposit(contractId, body: DepositBody)`; `getContract(contractId): Promise<ContractResponse>` → GET `rental-contracts/{id}`; `listContractsByVehicle(vehicleId, status?: ContractStatus): Promise<ContractResponse[]>` → GET `vehicles/{id}/rental-contracts` building URLSearchParams and only setting status when provided (MSW matches path only, Pitfall 6 — the handler reads searchParams itself).

Create `src/features/contracts/dateAlgiers.ts`: `const ALGIERS = "Africa/Algiers"`; `dayKeyAlgiers(iso: string): string` using `Intl.DateTimeFormat("en-CA", { timeZone: ALGIERS, year, month, day })` → "YYYY-MM-DD"; `isTodayAlgiers(iso: string, now: Date = new Date()): boolean` comparing dayKeyAlgiers(iso) to dayKeyAlgiers(now.toISOString()) — accept an injectable `now` so the boundary test can pin it; `toRFC3339Algiers(local: string): string` returning `${local}:00+01:00` (Algeria is fixed UTC+1, no DST — Pattern 2). Document why naive UTC slicing is off-by-one near midnight (Pitfall 3).

Create `src/features/contracts/resolve.ts`: `byId<T extends { id: string }>(items: T[]): Map<string, T>`; a `ContractView` interface { contract: ContractResponse; plate?: string; customerName?: string; agencyId?: string }; `toContractView(contract, vehicles: Map<string, VehicleResponse>, customers: Map<string, CustomerResponse>): ContractView` resolving plate = vehicle.registration_plate, agencyId = vehicle.agency_id, customerName = company→legal_name else individual→full_name. This is the single home for the UUID→name/plate/agency join every screen needs (04-RESEARCH.md backend reality #2 — contractResponse carries only UUIDs).

Create `src/features/contracts/queries.ts` (mirror fleet/queries.ts key namespacing, keys under ["contracts", ...] per D-06): `useContractQuery(id)` → ["contracts","detail",id] via getContract; `useContractsByVehicleQuery(vehicleId, status?)` → ["contracts","byVehicle",vehicleId,{status}]; `useContractsForVehicles(vehicleIds: string[], status: ContractStatus | null)` using `useQueries` with a `combine` that flattens `data`, ORs `isPending`/`isError` (04-RESEARCH.md "OPS-01 fan-out with useQueries"); `useAllContractsQuery()` = read `useVehiclesQuery(null)` (from features/fleet/queries) then fan out with status null over the returned vehicle ids and return the combined view PLUS the vehicles array (needed for the join); `useTodayOverviewQuery()` = fan out status="reserved" and status="active" over the vehicles, filter pickups by isTodayAlgiers(starts_at) and returns by isTodayAlgiers(ends_at). Do NOT invent a GET /contracts or /today endpoint (anti-pattern). Write `queries.test.tsx`, `dateAlgiers.test.ts`, `resolve.test.ts` covering the behavior block (queries via MSW + fresh QueryClient per test, mirroring customers/mutations.test.tsx harness). The dateAlgiers boundary test MUST assert the 23:30 UTC → next Algiers day case explicitly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/features/contracts/dateAlgiers.test.ts src/features/contracts/resolve.test.ts src/features/contracts/queries.test.tsx</automated>
  </verify>
  <done>Date helper, resolve join, and all query hooks green including the near-midnight Algiers boundary assertion and the parallel fan-out flatten/combine assertions; no invented list-all/today endpoint; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: mutations.ts — lifecycle mutations, dual-namespace invalidation, per-mutation 409 mapping</name>
  <files>src/features/contracts/mutations.ts, src/features/contracts/mutations.test.tsx</files>
  <behavior>
    - useCreateContract().mutateAsync({vehicleId, body}) POSTs create once and, onSuccess, invalidates ['contracts'] AND ['vehicles'] (D-06 dual-namespace — vehicle status changes on the lifecycle).
    - useActivate(id)/useClose(id)/useCancel(id)/useRecordDeposit(id) each POST their route and invalidate both ['contracts'] and ['vehicles'] on success.
    - overlapErrorKey(error): returns "contracts.errors.overlap" when isHTTPError(error) && status===409, else null — used ONLY by the create call.
    - transitionErrorKey(kind, error): for kind "activate"|"close"|"cancel", returns the distinct key ("contracts.errors.notReservable"|"notClosable"|"notCancellable") when status===409, else null — a create 409 and a transition 409 are mapped differently (Pitfall 2: 409 is ambiguous across mutations).
  </behavior>
  <action>
Create `src/features/contracts/mutations.ts` mirroring customers/mutations.ts idioms (D-06). Export TanStack mutations: `useCreateContract()` with mutationFn `({ vehicleId, body }: { vehicleId: string; body: CreateContractBody }) => createContract(vehicleId, body)` and onSuccess invalidating `{ queryKey: ["contracts"] }` then `{ queryKey: ["vehicles"] }` (04-RESEARCH.md Pattern 1 — both caches, because activate→rented / close→available / cancel(active)→available are vehicle-status side effects); `useActivate(contractId)`, `useClose(contractId)`, `useCancel(contractId)`, `useRecordDeposit(contractId)` each wrapping the matching api call with the SAME dual invalidation. Add pure helpers `overlapErrorKey(error: unknown): string | null` and `transitionErrorKey(kind: "activate" | "close" | "cancel", error: unknown): string | null` using `isHTTPError` from ky and `err.response.status === 409` (client.ts pattern) — these encode the per-mutation 409 disambiguation (D-04 + Pitfall 2): the SAME 409 status means "overlap" on create but "illegal status transition (stale UI)" on a lifecycle action; downstream callers (detail 04-03, wizard finish 04-05) will refetch the contract to re-gate after a transition 409. Never surface problem.detail verbatim (threat T-04-04). Write `mutations.test.tsx` (MSW + fresh QueryClient per test, spy on queryClient.invalidateQueries exactly like customers/mutations.test.tsx) covering: create invalidates both namespaces; each lifecycle mutation invalidates both; overlapErrorKey maps a 409 to the overlap key and null otherwise; transitionErrorKey maps each kind's 409 to its distinct key.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/features/contracts/mutations.test.tsx</automated>
  </verify>
  <done>All five mutations invalidate both ['contracts'] and ['vehicles'] on success; overlapErrorKey and transitionErrorKey return the correct distinct keys for a 409 and null otherwise; tsc clean; full `npx vitest run src/features/contracts` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → wheelio-api REST | All contract data + lifecycle actions cross here; untrusted route-param ids are interpolated into request paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | api.ts id interpolation | medium | mitigate | `encodeURIComponent` every vehicle/contract id before path interpolation (mirrors customers/api.ts) |
| T-04-03 | Tampering | create overlap / double-booking | high | accept | The DB EXCLUDE constraint + app pre-check are the authority (409); the client never authorizes a booking — it only maps the 409 to friendly copy |
| T-04-04 | Information Disclosure | 409/5xx error text | medium | mitigate | overlapErrorKey/transitionErrorKey return i18n keys; problem.detail is never rendered verbatim |
| T-04-05 | Tampering | stale-UI illegal transition | medium | mitigate | transitionErrorKey distinguishes a transition 409; callers refetch the contract to re-gate |
| T-04-SC | Tampering | npm installs | high | accept | Zero packages installed this phase (04-RESEARCH.md Package Legitimacy Audit: N/A) — no supply-chain surface introduced |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/features/contracts` and `npx vitest run src/test/mocks/handlers.test.ts` green.
- FR/EN key-set parity across contracts.*/wizard.*/ops.* (scripted count check).
- No invented list-all or /today endpoint; agency_id is never read off contractResponse.
</verification>

<success_criteria>
The rental data layer exists end-to-end: DTOs, api, read hooks with client-side composition, lifecycle mutations with dual invalidation and 409 disambiguation, Africa/Algiers date math, id→name/plate/agency resolution, MSW handlers + fixtures for every status + the overlap/today cases, and FR/EN copy — all green and tsc-clean, so 04-02..04-06 are pure composition.
</success_criteria>

<output>
Create `.planning/phases/04-contrats-de-location/04-01-SUMMARY.md` when done.
</output>
