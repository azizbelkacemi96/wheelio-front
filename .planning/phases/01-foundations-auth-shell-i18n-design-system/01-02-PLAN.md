---
phase: 01-foundations-auth-shell-i18n-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components.json
  - src/index.css
  - src/lib/utils.ts
  - src/shared/ui/theme-provider.tsx
  - src/shared/ui/theme-provider.test.tsx
  - src/shared/ui/button.tsx
  - src/shared/ui/input.tsx
  - src/shared/ui/label.tsx
  - src/shared/ui/form.tsx
  - src/shared/ui/card.tsx
  - src/shared/ui/dropdown-menu.tsx
  - src/shared/ui/avatar.tsx
  - src/shared/ui/separator.tsx
  - src/shared/ui/sonner.tsx
  - src/shared/ui/skeleton.tsx
  - src/shared/ui/badge.tsx
  - src/shared/ui/sheet.tsx
  - src/shared/ui/tooltip.tsx
autonomous: true
requirements: [AUTH-06]
gap_closure: false
must_haves:
  truths:
    - "Every screen consumes ONE documented design-token system: colors, spacing, and typography from 01-UI-SPEC.md are defined once as CSS custom properties, not per-component."
    - "Dark and light themes both ship from day one — token values exist under :root (light) and .dark (dark) for every declared role."
    - "Theme choice toggles the .dark class on the document element and persists across reload (storageKey wheelio-ui-theme)."
    - "Inter variable font is applied with tabular-figure support available for numeric table cells reused by later phases."
    - "The 13 base shadcn components (button, input, label, form, card, dropdown-menu, avatar, separator, sonner, skeleton, badge, sheet, tooltip) are vendored from the official registry."
  artifacts:
    - components.json
    - src/index.css
    - src/shared/ui/theme-provider.tsx
    - src/lib/utils.ts
  key_links:
    - "shadcn components reference token classes (bg-primary, text-foreground, border) that resolve to the CSS variables defined in src/index.css — one source of truth."
    - "theme-provider.tsx toggles the document element class; the .dark selector in index.css flips every token simultaneously."
  prohibitions:
    - "Do not hardcode 01-UI-SPEC.md token values as per-component arbitrary-value color utilities; register them once as CSS variables and consume via token classes (Pitfall 5)."
    - "Do not use the next-themes package; use shadcn's Vite-native ThemeProvider pattern."
    - "Do not introduce any 5th type size or 3rd font weight beyond the 4 sizes / 2 weights declared in 01-UI-SPEC.md Typography."
---

<objective>
Build the documented design-token system (AUTH-06): initialise shadcn/ui for Vite, wire every 01-UI-SPEC.md token (color/spacing/typography, light + dark) as CSS custom properties, add the Inter variable font, ship the Vite-native ThemeProvider (dark mode from day one per D-02), and vendor the 13 base components later plans compose with.

Purpose: AUTH-06 requires ONE documented token system applied consistently, not per-component styling. Every screen in this and every later phase renders through these tokens and components.
Output: components.json, a fully-tokenised src/index.css (light + dark), ThemeProvider with tests, and the base component library.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-UI-SPEC.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn init + Tailwind v4 token layer (light + dark), Inter font</name>
  <read_first>
    - 01-UI-SPEC.md "Design System" (style new-york, base slate, cssVariables true, RSC no), "Spacing Scale" (xs..3xl + dense exceptions), "Typography" (12/14/18/24, weights 400/600), "Color" (light/dark table incl. Success/Warning/Border tokens + accent-reserved rules + auth-screen gradient).
    - 01-RESEARCH.md "Pattern 5" and "Pitfall 5" (define tokens once as CSS variables matching shadcn's expected names; never a parallel hardcoded value), "Open Questions #2" (run `npx shadcn init` first, then read the generated index.css before hand-editing variable names).
  </read_first>
  <files>components.json, src/index.css, src/lib/utils.ts</files>
  <action>
    Run `npx shadcn@latest init` with style new-york, base color slate, CSS variables enabled, RSC disabled (Vite SPA) — this writes components.json and the shadcn variable scaffold into src/index.css and creates src/lib/utils.ts (the cn() helper wrapping clsx + tailwind-merge). Read the generated variable names in src/index.css first; do not guess them. Then map every 01-UI-SPEC.md token onto the shadcn CSS custom properties, defining values under both :root (light) and .dark (dark): background/foreground (white / slate-950 and slate surfaces), card + secondary surfaces (slate-100 / slate-900), primary + primary-foreground (blue-600 light / blue-500 dark — blue accent per D-03), destructive (red-600 / red-500), plus the downstream tokens Success (green-600 / green-500), Warning (amber-600 / amber-500), and border/muted (slate-200 / slate-800). Establish the spacing scale (with the dense/compact exceptions per D-04 — tight table rows, 44x44 touch targets, lg auth-screen padding) and the 4-size/2-weight type scale as documented tokens. Import @fontsource-variable/inter, set the body font stack Inter, system-ui, -apple-system, sans-serif, and define a reusable numeric-cell class enabling tabular figures (tnum) for the mileage/DZD columns later phases consume. Keep the auth-screen brand gradient (blue to indigo) as a single documented token/utility, used only on login/signup backgrounds (consumed in plan 05).
    The token layer is the single source of truth — colours are not repeated as raw values inside components.
  </action>
  <verify>
    <automated>npm run build && grep -Eq '(^|[^-])\.dark' src/index.css && echo dark-selector-present</automated>
    <human-check>Toggle the theme in the running app and reload: confirm no flash-of-unstyled-content and that both light and dark match 01-UI-SPEC.md's declared color tokens (AUTH-06 visual correctness — manual per 01-VALIDATION.md).</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; src/index.css defines the token set under both :root and .dark.
    - Colours are declared once as CSS variables; components reference token classes (bg-primary, text-foreground) rather than repeated raw values.
    - Exactly the 4 type sizes and 2 weights from 01-UI-SPEC.md exist; no others introduced.
  </acceptance_criteria>
  <done>The documented token system exists (light + dark), Inter is wired with tabular figures, and the build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Vite-native ThemeProvider + base component library</name>
  <read_first>
    - 01-RESEARCH.md "Code Examples > Full shadcn Vite ThemeProvider (dark mode)" (verbatim pattern, storageKey), "Anti-Patterns to Avoid" (no next-themes).
    - 01-UI-SPEC.md "Design System" (component list), "Registry Safety" (official registry only, no third-party registries).
    - 01-VALIDATION.md per-task map row AUTH-06 (theme toggle updates .dark class and persists across reload — component test, jsdom + localStorage mock).
  </read_first>
  <files>src/shared/ui/theme-provider.tsx, src/shared/ui/theme-provider.test.tsx, src/shared/ui/button.tsx, src/shared/ui/input.tsx, src/shared/ui/label.tsx, src/shared/ui/form.tsx, src/shared/ui/card.tsx, src/shared/ui/dropdown-menu.tsx, src/shared/ui/avatar.tsx, src/shared/ui/separator.tsx, src/shared/ui/sonner.tsx, src/shared/ui/skeleton.tsx, src/shared/ui/badge.tsx, src/shared/ui/sheet.tsx, src/shared/ui/tooltip.tsx</files>
  <action>
    Create src/shared/ui/theme-provider.tsx from 01-RESEARCH.md's verbatim shadcn Vite pattern: a context provider with theme 'dark'|'light'|'system', reading/writing localStorage under storageKey wheelio-ui-theme, toggling the light/dark class on the document element (respecting prefers-color-scheme for 'system'), and exporting useTheme(). Do NOT use next-themes.
    Vendor the 13 base components from the shadcn official registry via `npx shadcn@latest add button input label form card dropdown-menu avatar separator sonner skeleton badge sheet tooltip` into src/shared/ui/ (deps cva/tailwind-merge/clsx/lucide-react are already installed in plan 01, so this vendors files only).
    Write src/shared/ui/theme-provider.test.tsx (jsdom + a localStorage mock): setTheme('dark') adds the dark class to the document element and writes wheelio-ui-theme; re-mounting reads the persisted value back (persists across reload); setTheme('light') removes the dark class.
  </action>
  <verify>
    <automated>npx vitest run src/shared/ui/theme-provider.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Theme toggle adds/removes the dark class on the document element and persists the choice to localStorage under wheelio-ui-theme.
    - All 13 base components exist under src/shared/ui/ and typecheck.
    - No next-themes import anywhere.
  </acceptance_criteria>
  <done>ThemeProvider works and is tested; the base component library is vendored from the official registry.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New files created by this plan (all net-new):
- components.json (shadcn config)
- src/index.css (fully tokenised: :root + .dark, spacing/type scale, Inter font, tnum numeric utility, auth gradient token)
- src/lib/utils.ts (exports cn())
- src/shared/ui/theme-provider.tsx (exports ThemeProvider, useTheme)
- src/shared/ui/{button,input,label,form,card,dropdown-menu,avatar,separator,sonner,skeleton,badge,sheet,tooltip}.tsx (vendored shadcn components + their exports)
These symbols are newly created here — legitimate new artifacts for the source-grounding drift-guard.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn/npm registry -> vendored components | Component source pulled into the repo |
| stored theme pref (localStorage) -> DOM class | Only a non-sensitive UI preference crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | shadcn add / component vendoring | high | mitigate | Only the shadcn OFFICIAL registry is used (01-UI-SPEC.md Registry Safety declares no third-party registries); vendored component source is committed and reviewable. cva/tailwind-merge/clsx/lucide-react were audited Approved in 01-RESEARCH.md. |
| T-01-theme | Tampering | theme-provider localStorage | low | accept | Only a UI theme preference is stored; no security value. Reading an attacker-set value at worst renders the wrong theme. |
</threat_model>

<verification>
- `npm run build` green; token layer defines :root + .dark.
- `npx vitest run src/shared/ui/theme-provider.test.tsx` green.
- Manual: theme visual correctness / no FOUC (human-check above).
</verification>

<success_criteria>
One documented token system (light + dark) applied via CSS variables; ThemeProvider tested; 13 base components vendored from the official registry.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-auth-shell-i18n-design-system/01-02-SUMMARY.md` when done.
</output>
