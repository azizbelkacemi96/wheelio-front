---
phase: 01-foundations-auth-shell-i18n-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - vitest.config.ts
  - playwright.config.ts
  - index.html
  - .gitignore
  - .env.example
  - src/main.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/routes/__root.tsx
  - src/routes/index.tsx
  - src/test/setup.ts
  - src/test/mocks/handlers.ts
  - src/test/mocks/server.ts
  - src/test/fixtures/scope.ts
  - src/types/identity.ts
autonomous: true
requirements: [AUTH-01, AUTH-03]
gap_closure: false
must_haves:
  truths:
    - "`npx tsc --noEmit` passes and `npm run build` produces a bundle from a bare-clone checkout (greenfield scaffold is buildable)."
    - "`npx vitest run` executes at least one passing test with the MSW server bootstrapped in src/test/setup.ts (no network escapes)."
    - "src/types/identity.ts mirrors the wheelio-api DTO contract exactly (UserResponse.org_role is owner|admin|member; MembershipResponse.role is manager|agent|viewer; AuthResponse carries access_token, access_token_expires_at, refresh_token)."
    - "MSW handlers return the confirmed shapes for POST /auth/login, /auth/signup, /auth/refresh, /auth/logout, GET /me, GET /agencies."
    - "Scope fixtures exist for all three role shapes (agent, gérant/manager, owner) reusable by permissions and shell tests."
  artifacts:
    - package.json
    - vite.config.ts
    - vitest.config.ts
    - playwright.config.ts
    - src/types/identity.ts
    - src/test/setup.ts
    - src/test/mocks/handlers.ts
    - src/test/mocks/server.ts
    - src/test/fixtures/scope.ts
  key_links:
    - "src/test/setup.ts imports and starts src/test/mocks/server.ts (beforeAll/afterEach/afterAll) — every later test inherits the mocked API."
    - "vite.config.ts resolve alias @/* -> src/* is honored by both the build and vitest (shared config), or duplicated in vitest.config.ts."
    - "src/test/mocks/handlers.ts response bodies are typed against src/types/identity.ts so contract drift fails compilation."
  prohibitions:
    - "Do not store the access token in localStorage or sessionStorage anywhere in scaffolded config (established here, enforced in plan 03)."
    - "Do not commit real secrets — only .env.example with VITE_API_URL placeholder is created."
    - "Do not add i18next-icu or next-themes to package.json (research anti-patterns; not used this phase)."
---

<objective>
Scaffold the greenfield Vite + React 19 + TypeScript SPA, its build/test tooling, and the API DTO contract types + MSW mock layer that every later plan in this phase depends on. This is Wave 0 per 01-VALIDATION.md: nothing exists in the repo yet (no package.json, no src/), so this plan stands up the buildable, testable skeleton.

Purpose: Every other plan (design system, auth core, i18n, screens, shell) mounts onto this scaffold. The test framework, MSW handlers, and Scope fixtures created here are the Wave 0 prerequisites 01-VALIDATION.md lists as MISSING.
Output: A buildable app skeleton, green test harness, DTO types mirroring wheelio-api, and reusable API mocks + role fixtures.

Note (auditability): The orchestrator's requirement-side edge-probe fired on AUTH-01..06 and correctly abstained/mismatched — this is an auth/UI-flow phase, not a data-shape phase, so those 9 probe rows are noise and were NOT used to author must_haves. UI-state coverage was lifted from 01-UI-SPEC.md's `## UI Considerations` instead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-RESEARCH.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React 19 + TypeScript app and build tooling</name>
  <read_first>
    - 01-RESEARCH.md "Installation" block (exact package set), "Recommended Project Structure" (folder layout), "Environment Availability" (Node >=20 for Vite 8).
    - 01-RESEARCH.md "Package Legitimacy Audit" (all core packages Approved; SUS flags are recency-heuristic false positives — no human-verify gate needed for react/vite/typescript per the researcher's documented decision).
  </read_first>
  <files>package.json, tsconfig.json, tsconfig.node.json, vite.config.ts, index.html, .gitignore, .env.example, src/main.tsx, src/index.css, src/vite-env.d.ts, src/routes/__root.tsx, src/routes/index.tsx</files>
  <action>
    Scaffold a Vite + React 19 + TypeScript SPA. Create package.json installing the full runtime + dev dependency set from 01-RESEARCH.md at the pinned versions: react@19, react-dom@19, @tanstack/react-router (1.170.x), @tanstack/react-query, zustand (5.0.x), i18next (26.3.x), react-i18next (17.0.x), i18next-browser-languagedetector, ky (2.0.x), tailwindcss (4.3.x), @tailwindcss/vite, react-hook-form, zod, @hookform/resolvers, @fontsource-variable/inter (5.3.x), lucide-react, class-variance-authority, tailwind-merge, clsx; dev: vite, typescript, @vitejs/plugin-react, @tanstack/router-plugin, vitest (4.1.x), @testing-library/react, @testing-library/jest-dom, @testing-library/user-event, jsdom, msw, @playwright/test. Pre-installing cva/tailwind-merge/clsx/lucide-react here means plan 02's `npx shadcn add` vendors component files only and does not re-touch package.json.
    Configure tsconfig.json with strict mode and path alias `@/*` -> `src/*`; tsconfig.node.json for the Vite config. vite.config.ts wires @vitejs/plugin-react, @tailwindcss/vite, the @tanstack/router-plugin (file-based routing, routesDirectory src/routes, generatedRouteTree src/routeTree.gen.ts), and resolve.alias `@` -> `/src`. Add VITE_API_URL typing to src/vite-env.d.ts (ImportMetaEnv) and .env.example with `VITE_API_URL=http://localhost:8080`. index.html mounts `#root`. src/main.tsx renders a minimal RouterProvider skeleton and imports `./index.css`; src/index.css contains `@import "tailwindcss";` only (plan 02 fills the token layer). Create minimal src/routes/__root.tsx (Outlet) and src/routes/index.tsx (temporary landing) so the router builds. Create the empty folder structure: src/app, src/app/shell, src/features/auth, src/shared/api, src/shared/auth, src/shared/ui, src/shared/i18n, src/types, src/test. .gitignore excludes node_modules, dist, src/routeTree.gen.ts, .env.
    Add npm scripts: `dev`, `build` (tsc -b && vite build), `preview`, `test` (vitest run), `test:e2e` (playwright test).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` produces dist/.
    - Path alias `@/` resolves in both build and (Task 2) vitest.
    - package.json contains NO i18next-icu and NO next-themes entry.
  </acceptance_criteria>
  <done>The app builds clean from a fresh clone; folder skeleton and env config exist; dependency set matches 01-RESEARCH.md.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vitest + Playwright + MSW test harness</name>
  <read_first>
    - 01-VALIDATION.md "Test Infrastructure" (Vitest 4.1.10 + Playwright 1.61.1 + Testing Library + MSW), "Wave 0 Requirements" (config files + setup + install list), "Sampling Rate" (max feedback latency 90s, no watch-mode flags).
    - 01-RESEARCH.md "Validation Architecture" (quick/full run commands, Wave 0 gaps).
  </read_first>
  <files>vitest.config.ts, playwright.config.ts, src/test/setup.ts, src/test/mocks/server.ts</files>
  <action>
    Create vitest.config.ts (environment jsdom, globals true, setupFiles ['src/test/setup.ts'], the same `@` alias, reporter default — no watch flags). Create playwright.config.ts (testDir e2e/, baseURL from VITE_API_URL of the preview server, webServer running `npm run preview` or `npm run dev`, single chromium project for now). Create src/test/mocks/server.ts exporting `setupServer(...handlers)` from msw/node importing handlers from ./handlers (handlers file is created in Task 3 — this import is the seam). src/test/setup.ts imports @testing-library/jest-dom, imports the MSW server, and registers beforeAll(server.listen {onUnhandledRequest:'error'}), afterEach(server.resetHandlers), afterAll(server.close). Add a trivial smoke test (e.g. src/test/setup.smoke.test.ts asserting the jsdom document exists) so the harness has something to run green.
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` completes with at least one passing test and the MSW server active (onUnhandledRequest set to 'error').
    - No test uses a watch flag; full unit run finishes well under 90s.
  </acceptance_criteria>
  <done>Vitest + Playwright configs exist; MSW server bootstraps in setup; smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 3: API DTO types, MSW handlers, and per-role Scope fixtures</name>
  <read_first>
    - 01-RESEARCH.md "Code Examples > /me DTO types mirroring the API exactly" (verbatim UserResponse/OrganizationResponse/MembershipResponse/MeResponse/AuthResponse) and the confirmed endpoint list (/auth/login, /auth/signup, /auth/refresh, /auth/logout, /me, /agencies).
    - /Users/azizbelkacemi/Desktop/work-dev/wheelio-api/internal/adapter/httpapi/dto.go (request/response field names + json tags — read-only reference).
    - /Users/azizbelkacemi/Desktop/work-dev/wheelio-api/internal/domain/identity/scope.go (role ranks: viewer<agent<manager; org admins implicit manager everywhere) for building realistic fixtures.
  </read_first>
  <files>src/types/identity.ts, src/test/mocks/handlers.ts, src/test/fixtures/scope.ts</files>
  <action>
    Create src/types/identity.ts with the DTO interfaces mirroring wheelio-api 1:1: UserResponse (id, email, first_name, last_name, org_role: 'owner'|'admin'|'member', is_active, created_at), OrganizationResponse (id, name, created_at), MembershipResponse (user_id, agency_id, role: 'manager'|'agent'|'viewer', created_at), MeResponse (user, organization, memberships[]), AuthResponse (token_type:'Bearer', access_token, access_token_expires_at ISO string, refresh_token, user, organization), and AgencyResponse (id, name, ... the GET /agencies item shape) for the switcher. Add a header comment pointing to wheelio-api dto.go as the source of truth so drift is traceable.
    Create src/test/mocks/handlers.ts: MSW http handlers returning typed bodies for POST /auth/login and /auth/signup (AuthResponse), POST /auth/refresh (rotated access_token + refresh_token + access_token_expires_at), POST /auth/logout (204), GET /me (MeResponse), GET /agencies (AgencyResponse[]). Base the URL on VITE_API_URL. Bodies typed against src/types/identity.ts so any contract drift fails compilation.
    Create src/test/fixtures/scope.ts exporting ready-made MeResponse fixtures and derived Scope shapes for three roles: an agent (single membership role 'agent'), a gérant/manager (membership role 'manager'), and an owner (org_role 'owner', memberships may be empty since org admins are implicit manager everywhere). Include at least one multi-agency owner fixture for the switcher tests.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/test</automated>
  </verify>
  <acceptance_criteria>
    - src/types/identity.ts compiles and the union literals match dto.go json tag values exactly.
    - handlers.ts and scope.ts import cleanly; a test importing a handler and a fixture runs green.
    - Owner fixture has org_role 'owner'; agent/manager fixtures carry the corresponding membership role.
  </acceptance_criteria>
  <done>DTO types, MSW handlers, and per-role Scope fixtures exist and typecheck; the Wave 0 test-data layer 01-VALIDATION.md requires is complete.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New files created by this plan (all net-new — greenfield repo):
- Config: package.json, tsconfig.json, tsconfig.node.json, vite.config.ts, vitest.config.ts, playwright.config.ts, index.html, .gitignore, .env.example
- App entry: src/main.tsx, src/index.css, src/vite-env.d.ts, src/routes/__root.tsx, src/routes/index.tsx
- Types: src/types/identity.ts — exports interfaces UserResponse, OrganizationResponse, MembershipResponse, MeResponse, AuthResponse, AgencyResponse
- Test infra: src/test/setup.ts, src/test/mocks/server.ts (exports `server`), src/test/mocks/handlers.ts (exports `handlers`), src/test/fixtures/scope.ts (exports per-role MeResponse + Scope fixtures)
These symbols are newly created here — the plan-review source-grounding drift-guard must treat them as legitimate new artifacts, not hallucinations.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local build | Third-party packages installed into the toolchain |
| browser JS -> wheelio-api | All auth/data requests cross here (mocked by MSW in tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (package.json) | high | mitigate | 01-RESEARCH.md "Package Legitimacy Audit" already ran the legitimacy gate: every package resolved to a canonical, high-download repo; all SUS verdicts are documented recency-heuristic false positives; zero SLOP packages. No new/unaudited package is introduced. Per the researcher's explicit decision, mechanically gating react/vite/typescript behind a human-verify checkpoint is unnecessary overhead here — no [ASSUMED]/[SLOP] package requires a blocking checkpoint. |
| T-01-xss | Information Disclosure | scaffold/config | medium | mitigate | Access token will live in memory only (established in plan 03); scaffold introduces no persistent token storage and no dangerouslySetInnerHTML. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run build` clean.
- `npx vitest run` green with MSW active (onUnhandledRequest:'error').
- DTO union literals match wheelio-api dto.go json tag values.
</verification>

<success_criteria>
Buildable Vite+React19+TS skeleton; green Vitest/Playwright/MSW harness; DTO types + mocks + role fixtures ready for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-auth-shell-i18n-design-system/01-01-SUMMARY.md` when done.
</output>
