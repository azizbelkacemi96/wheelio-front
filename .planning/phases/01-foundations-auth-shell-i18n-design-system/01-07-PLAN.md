---
phase: 01-foundations-auth-shell-i18n-design-system
plan: 07
type: execute
wave: 4
depends_on: ["01-05", "01-06"]
files_modified:
  - src/shared/ui/empty-state.tsx
  - src/routes/_authenticated/index.tsx
  - src/routes/_authenticated/vehicules.tsx
  - src/routes/_authenticated/clients.tsx
  - src/routes/_authenticated/contrats.tsx
  - src/routes/_authenticated/etats-des-lieux.tsx
  - src/routes/_authenticated/admin/identite-fiscale.tsx
  - src/routes/_authenticated/admin/agences.tsx
  - src/routes/_authenticated/admin/facturation.tsx
  - src/routes/_authenticated/placeholders.test.tsx
  - e2e/auth.spec.ts
autonomous: true
requirements: [AUTH-03, AUTH-05]
gap_closure: false
must_haves:
  truths:
    - "Every base nav destination (Aujourd'hui, Véhicules, Clients, Contrats, États des lieux) resolves to a route that renders the shared empty state ('Bientôt disponible' / 'Coming soon' + body) rather than a dead link or blank page."
    - "The three admin placeholder routes exist under an owner-only segment and render the same empty state; they are reachable only through the org-admin nav sections."
    - "Empty-state copy comes from i18n keys and reflects the selected locale (FR default, EN on switch)."
    - "An end-to-end happy path passes: log in -> land in the authenticated shell -> the role-gated nav is present -> navigating a base section shows the placeholder."
  artifacts:
    - src/shared/ui/empty-state.tsx
    - src/routes/_authenticated/index.tsx
    - e2e/auth.spec.ts
  key_links:
    - "Placeholder routes are children of the _authenticated layout (plan 06), so they inherit the guard + AppShell chrome automatically."
    - "empty-state.tsx reads the 'Bientôt disponible'/'Coming soon' keys from i18n (plan 04)."
    - "e2e/auth.spec.ts drives the plan-05 login form and asserts the plan-06 shell + nav render."
  prohibitions:
    - "Do not build any real feature functionality in the placeholders; they render the empty state only (feature screens belong to later phases)."
    - "Do not expose the admin placeholder routes in the base nav; they are reachable only via the owner-only admin sections (D-09)."
    - "Do not hardcode the placeholder copy as bare JSX literals; use i18n keys (plan 04)."
---

<objective>
Close the shell loop: give every nav destination a real (placeholder) route so navigation never dead-ends, and prove the whole phase works end-to-end with a Playwright happy path (login -> shell -> role-gated nav -> placeholder). This is the integration plan that depends on both the auth screens (plan 05) and the shell (plan 06).

Purpose: Success Criteria for the phase require that navigation and actions match the user's role and that every screen reflects the language choice — the placeholder pages make the nav navigable and locale-reactive, and the E2E test is the phase's top-level proof.
Output: a shared EmptyState, the base + admin placeholder routes, and e2e/auth.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-UI-SPEC.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-VALIDATION.md
@src/app/shell/AppShell.tsx
@src/routes/_authenticated.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared EmptyState + base & admin placeholder routes</name>
  <read_first>
    - 01-UI-SPEC.md "Copywriting Contract" (empty-state heading 'Bientôt disponible'/'Coming soon' + body copy), "UI Considerations" (placeholder/stub pages empty-state row).
    - CONTEXT.md D-08 (base nav sections) and D-09 (three owner-only admin sections).
    - 01-RESEARCH.md "Recommended Project Structure" (routes under the _authenticated layout).
  </read_first>
  <files>src/shared/ui/empty-state.tsx, src/routes/_authenticated/index.tsx, src/routes/_authenticated/vehicules.tsx, src/routes/_authenticated/clients.tsx, src/routes/_authenticated/contrats.tsx, src/routes/_authenticated/etats-des-lieux.tsx, src/routes/_authenticated/admin/identite-fiscale.tsx, src/routes/_authenticated/admin/agences.tsx, src/routes/_authenticated/admin/facturation.tsx, src/routes/_authenticated/placeholders.test.tsx</files>
  <action>
    Create src/shared/ui/empty-state.tsx — a small presentational component rendering a centered heading + body from i18n keys (accepts optional title/description key overrides, defaulting to the 'Bientôt disponible'/'Coming soon' + body copy). Create the base placeholder routes as children of the _authenticated layout (plan 06): index.tsx (Aujourd'hui landing), vehicules.tsx, clients.tsx, contrats.tsx, etats-des-lieux.tsx — each rendering EmptyState. Create the three admin placeholder routes under _authenticated/admin/ (identite-fiscale.tsx, agences.tsx, facturation.tsx) also rendering EmptyState; these are linked only from the owner-only admin nav sections (D-09), not the base nav. Write placeholders.test.tsx asserting a base placeholder route renders the FR empty-state copy by default and the EN copy after a locale switch.
  </action>
  <verify>
    <automated>npx vitest run src/routes/_authenticated/placeholders.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Every base nav destination resolves to a route rendering EmptyState; no dead links.
    - Admin placeholder routes exist under the owner-only segment and render EmptyState.
    - Empty-state copy is locale-reactive (FR default, EN after switch).
  </acceptance_criteria>
  <done>All nav destinations are navigable placeholders using the shared, i18n-driven empty state.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end happy path (login -> shell -> role-gated nav)</name>
  <read_first>
    - 01-VALIDATION.md per-task map row AUTH-01/02/03 E2E ("Full login -> shell -> role-gated nav happy path", `npx playwright test e2e/auth.spec.ts`), "Test Infrastructure" (Playwright config from plan 01).
    - 01-RESEARCH.md "System Architecture Diagram" (the boot -> login -> shell flow the E2E exercises), "Environment Availability" (live wheelio-api needed for real auth, or a mock).
  </read_first>
  <files>e2e/auth.spec.ts</files>
  <action>
    Write e2e/auth.spec.ts (Playwright) exercising the happy path against the app served by the plan-01 playwright.config webServer: visit /login, fill valid credentials, submit, assert redirection into the authenticated shell, assert the base nav items are visible, and assert that navigating a base section (e.g. Véhicules) shows the placeholder empty state. Drive against a running wheelio-api (VITE_API_URL) when available; if the live API is not available at execution time, back the E2E with a route-mock/fixture matching the confirmed auth/me shapes so the happy path still runs deterministically — document which mode was used in the SUMMARY. Keep assertions role-aware: as an org admin the agency switcher + admin sections are visible; as an agent they are absent.
  </action>
  <verify>
    <automated>npx playwright test e2e/auth.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The login -> shell -> nav -> placeholder happy path passes headless.
    - Role-aware assertions hold (admin sees switcher + admin sections; agent does not).
  </acceptance_criteria>
  <done>The phase's top-level E2E proof is green; the foundation is demonstrably usable end-to-end.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New files created by this plan (all net-new):
- src/shared/ui/empty-state.tsx — exports EmptyState
- src/routes/_authenticated/index.tsx (Aujourd'hui), vehicules.tsx, clients.tsx, contrats.tsx, etats-des-lieux.tsx — base placeholder routes
- src/routes/_authenticated/admin/identite-fiscale.tsx, agences.tsx, facturation.tsx — owner-only placeholder routes
- e2e/auth.spec.ts — the phase happy-path E2E
These are newly created here — legitimate new symbols for the source-grounding drift-guard.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner-only admin routes -> render | Reachable only via org-admin nav; backend re-authorises any real action later |
| E2E test harness -> app/API | Test-only; drives login against the real or mocked API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-rbac | Elevation of Privilege | admin placeholder routes | medium | mitigate | Admin placeholder routes are nav-gated (org-admin only) as UX; they contain no data or mutating action this phase. Real per-action authorization is enforced by the backend when those features ship in later phases. The E2E asserts an agent does not see the admin nav. |
| T-01-e2e-secret | Information Disclosure | e2e/auth.spec.ts credentials | low | mitigate | E2E credentials come from env/fixtures, never hardcoded real secrets; no token is logged. |
</threat_model>

<verification>
- `npx vitest run src/routes/_authenticated/placeholders.test.tsx` green.
- `npx playwright test e2e/auth.spec.ts` green.
- Full phase gate: `npx vitest run && npx playwright test` green before /gsd-verify-work.
</verification>

<success_criteria>
Every nav destination is a navigable, locale-reactive placeholder; the login -> shell -> role-gated nav happy path passes end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-auth-shell-i18n-design-system/01-07-SUMMARY.md` when done.
</output>
