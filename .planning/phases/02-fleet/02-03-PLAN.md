---
phase: 02-fleet
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/features/fleet/VehicleDetail.tsx
  - src/features/fleet/VehicleDetail.test.tsx
  - src/routes/_authenticated/vehicules/$vehicleId.tsx
  - src/app/router.test.tsx
  - src/routeTree.gen.ts
autonomous: true
requirements: [FLEET-02]

must_haves:
  truths:
    - "/vehicules/{id} shows the vehicle's plate, brand/model, locale-formatted mileage, translated fuel type, transmission, and current status badge"
    - "Optional vehicle fields (model_year, color, seats, notes) render only when present — an absent key never renders as 'undefined' or crashes formatting"
    - "When an active contract exists, a current-contract card shows the rental period, contract status, departure mileage, and departure fuel level (the ONLY fuel-level reading the API has)"
    - "When the contracts query returns an empty array, the card shows the noCurrentContract copy — no value is invented from vehicle.status"
    - "An unknown or out-of-scope vehicle id renders the not-found state with a working back-to-list link — no crash, and no forbidden-vs-missing distinction"
  artifacts:
    - "src/features/fleet/VehicleDetail.tsx"
    - "src/features/fleet/VehicleDetail.test.tsx"
    - "src/routes/_authenticated/vehicules/$vehicleId.tsx"
  key_links:
    - "Detail composes useVehicleQuery + useActiveContractQuery from plan 02-01 — two parallel queries, no waterfall conditional on vehicle.status"
    - "Fuel LEVEL comes exclusively from the active contract's departure_fuel_level; the vehicle card shows fuel_type (vehicles have no fuel-level attribute — 02-RESEARCH.md verified)"
    - "$vehicleId.tsx route nests under the vehicules/ directory created by plan 02-02 and reads the id via Route.useParams"
---

<objective>
Ship FLEET-02: the vehicle detail page at /vehicules/$vehicleId (D-04) — vehicle identity card (plate, brand/model, mileage, fuel type, transmission, status badge) plus a current-contract summary card resolved from the rental-contracts endpoint, with D-09 loading/error states and a not-found fallback.

Purpose: ROADMAP Phase 2 success criterion 2 — "User can open a vehicle's detail page and see its plate, brand/model, mileage, fuel, and current contract if one exists." Read-only per D-05; visible to all roles per D-08; customer identity is deliberately omitted from the contract summary (contractResponse carries only customer_id — customer endpoints are Phase 3; research Open Question 2).
Output: VehicleDetail component + tests, $vehicleId route, extended router test, phase-gate green suite + build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-fleet/02-RESEARCH.md
@.planning/phases/02-fleet/02-CONTEXT.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-UI-SPEC.md
@.planning/phases/02-fleet/02-01-SUMMARY.md
@.planning/phases/02-fleet/02-02-SUMMARY.md
@src/features/fleet/queries.ts
@src/features/fleet/StatusBadge.tsx
@src/test/fixtures/fleet.ts
@src/routes/_authenticated/vehicules/index.tsx
@src/app/router.test.tsx
@src/shared/i18n/useLocale.ts
@src/shared/ui/card.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build VehicleDetail with vehicle card + current-contract summary</name>
  <files>src/features/fleet/VehicleDetail.tsx, src/features/fleet/VehicleDetail.test.tsx</files>
  <behavior>
    - Given the rented fixture vehicle's id, renders plate, brand + model, mileage formatted with toLocaleString + km, translated fuel type, translated transmission, and the StatusBadge for its status
    - Given the omitempty-absent fixture vehicle, the model_year/color/seats/notes rows are simply not rendered (or show "—") and nothing renders the string "undefined"
    - Given the rented vehicle (active contract fixture exists), the current-contract card renders the period (formatted starts_at → ends_at), the translated contract status, departure mileage, and the translated departure fuel level
    - Given an available vehicle (contracts endpoint returns []), the card shows the vehicles.detail.noCurrentContract copy
    - Given an id MSW answers 404 for, the not-found state renders vehicles.detail.notFoundHeading/-Body plus a back link targeting /vehicules
    - While queries are pending, skeletons render; a 500 on the vehicle query shows the vehicles.loadError banner with a working retry
  </behavior>
  <action>
    `src/features/fleet/VehicleDetail.tsx` receiving `vehicleId: string` as a prop (route passes it), composing the two plan 02-01 hooks IN PARALLEL — `useVehicleQuery(vehicleId)` + `useActiveContractQuery(vehicleId)` — per the research composition strategy: the contract query is unconditional (an available vehicle just returns [], one cheap request), and a current contract is NEVER inferred from vehicle.status (anti-pattern).

    Layout (existing Card / Separator / Skeleton / Button primitives, 01-UI-SPEC dense spacing):
    - Header: back `Link` to `/vehicules` labelled `vehicles.detail.backToList`, then plate as the page heading with brand + model (+ model_year when present) as subline, and `<StatusBadge status={vehicle.status} />` alongside (D-02).
    - Vehicle info card (`vehicles.detail.infoTitle`): rows for VIN, mileage (`numeric-cell`, `current_mileage.toLocaleString(locale)` + " km", locale via `useLocale()`), fuel type (`t(`vehicles.fuelType.${vehicle.fuel_type}`)`), transmission, and — only when the key is present — model_year, color, seats, notes (Pitfall 3: these are absent JSON keys, not null; guard with presence checks before any formatting call). The vehicle card never shows a fuel LEVEL — vehicles don't have one (research "Fuel level reality").
    - Current-contract card (`vehicles.detail.currentContract`): when `useActiveContractQuery` data is non-null → period line (`vehicles.detail.contractPeriod`: format starts_at/ends_at via `new Date(...).toLocaleDateString(locale)`), contract status via `t(`contracts.status.${contract.status}`)`, departure mileage (`vehicles.detail.departureMileage`, numeric-cell) and departure fuel level (`vehicles.detail.departureFuel`, `t(`vehicles.fuelLevel.${contract.departure_fuel_level}`)` — guard for presence even though activation always sets it). When data is null → `vehicles.detail.noCurrentContract`. Customer identity intentionally omitted (only customer_id exists this phase). If only the contract query errors, show `vehicles.detail.contractLoadError` inline in the card with retry — don't blank the whole page.
    - States (D-09): vehicle query pending → skeleton blocks; vehicle query 404 (ky HTTPError with response.status 404) → not-found state (`vehicles.detail.notFoundHeading`/`notFoundBody` + back link) — the backend deliberately 404s out-of-scope vehicles, so never render an access-denied variant; other vehicle-query errors → `vehicles.loadError` banner + `vehicles.retry` button calling refetch.
    - Copy discipline (D-07): all labels through `t()`; raw data values render directly.

    `src/features/fleet/VehicleDetail.test.tsx`: cover every behavior line using the plan 02-01 fixtures and MSW handlers (per-test `server.use` overrides for the 404/500/empty-contract cases); fresh QueryClientProvider (retry: false) per test; minimal router harness for the Link assertions (same approach as VehicleList.test.tsx). RED first, then implement.
  </action>
  <verify>
    <automated>cd /Users/azizbelkacemi/Desktop/work-dev/wheelio-front && npx vitest run src/features/fleet && npx tsc -b</automated>
  </verify>
  <done>All behavior-block assertions pass: full vehicle card, presence-guarded optional fields, contract summary with departure fuel level, noCurrentContract state, not-found state, loading/error states.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the $vehicleId route and close the phase gate</name>
  <files>src/routes/_authenticated/vehicules/$vehicleId.tsx, src/app/router.test.tsx, src/routeTree.gen.ts</files>
  <action>
    1. Create `src/routes/_authenticated/vehicules/$vehicleId.tsx`: `createFileRoute("/_authenticated/vehicules/$vehicleId")` whose component reads `const { vehicleId } = Route.useParams()` and renders `<VehicleDetail vehicleId={vehicleId} />`. No loader (component-level useQuery is the established idiom). This is the D-04 route shape; it nests inside the vehicules/ directory plan 02-02 created, alongside index.tsx.
    2. Run `npm run generate-routes`; commit the regenerated `src/routeTree.gen.ts` containing both /vehicules and /vehicules/$vehicleId.
    3. Extend `src/app/router.test.tsx`: from the authenticated harness, navigate to `/vehicules/{id}` for a known fixture vehicle id and assert the detail screen renders that vehicle's plate (and the pathname matches); this proves list → detail routing works through the real route tree with the plan 02-01 MSW handlers.
    4. Phase gate: run the full suite and build; both must be green before the SUMMARY is written.
  </action>
  <verify>
    <automated>cd /Users/azizbelkacemi/Desktop/work-dev/wheelio-front && npm test && npm run build</automated>
  </verify>
  <done>/vehicules/$vehicleId resolves through the generated route tree to VehicleDetail; router test covers it; `npm test` and `npm run build` fully green (phase gate per 02-RESEARCH.md Validation Architecture).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → app | user-controlled $vehicleId path param forwarded to the API |
| API → browser | vehicle/contract strings (plate, notes) rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Information disclosure | IDOR probing /vehicules/$vehicleId | medium | transfer | backend org+membership authorize returns 404 for out-of-scope ids (wheelio-api service.go); frontend renders one generic not-found state, never confirming existence |
| T-02-07 | Tampering (XSS) | detail cards rendering API strings | medium | mitigate | React JSX auto-escaping; no dangerouslySetInnerHTML |
| T-02-08 | Information disclosure | customer_id exposure in contract summary | low | mitigate | customer identity omitted from the rendered summary entirely this phase (research Open Question 2 prescription) |

No package-manager installs — no supply-chain checkpoint required.
</threat_model>

<verification>
- `npm test` fully green (unit + component + route suites, regenerated tree).
- `npm run build` green (tsc -b catches any DTO/enum drift introduced late).
- Surface at verify time (research Open Question 1): the backend tracks fuel LEVEL per contract, not per vehicle — the list shows fuel type and the detail shows the active contract's departure fuel level. This is contract fidelity, not a UI omission.
</verification>

<success_criteria>
- ROADMAP Phase 2 criterion 2 satisfied against mocks: detail page shows plate, brand/model, mileage, fuel, status, and the current contract when one exists.
- Both phase success criteria now demonstrable end-to-end through the real route tree; suite + build green as the phase gate before /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/02-fleet/02-03-SUMMARY.md` when done.
</output>
