---
phase: 02-fleet
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/shared/ui/table.tsx
  - src/shared/ui/select.tsx
  - src/features/fleet/VehicleList.tsx
  - src/features/fleet/VehicleList.test.tsx
  - src/routes/_authenticated/vehicules/index.tsx
  - src/routes/_authenticated/vehicules.tsx
  - src/routes/_authenticated/placeholders.test.tsx
  - src/app/router.test.tsx
  - src/routeTree.gen.ts
autonomous: true
requirements: [FLEET-01]

must_haves:
  truths:
    - "/vehicules renders every scoped vehicle as dense table rows (md+) and stacked cards (<md) showing plate, brand/model, status badge, mileage, fuel type, and agency (owner view)"
    - "Selecting a status refetches the list with ?status=; typing in the search box filters client-side on plate/brand/model without any network call"
    - "Loading shows skeleton rows, a fetch error shows an inline banner with a working Réessayer button, zero vehicles shows EmptyState, and a filter that matches nothing shows the distinct noResults copy"
    - "Each row links to /vehicules/{id}"
    - "The Phase 1 placeholder route file for /vehicules is gone, route generation still succeeds, and placeholders.test.tsx no longer asserts vehicules renders the coming-soon EmptyState"
  artifacts:
    - "src/features/fleet/VehicleList.tsx"
    - "src/features/fleet/VehicleList.test.tsx"
    - "src/routes/_authenticated/vehicules/index.tsx"
    - "src/shared/ui/table.tsx"
    - "src/shared/ui/select.tsx"
  key_links:
    - "routes/_authenticated/vehicules/index.tsx registers VehicleList as its component — the route renders the real screen, not EmptyState"
    - "VehicleList consumes useVehiclesQuery from plan 02-01 (never a private fetch) and StatusBadge for every status cell"
    - "Agency column names resolve client-side from useAuthStore agencies (no agency name exists in vehicleResponse); column hidden when the store's agencies list is empty (non-admins)"
---

<objective>
Ship FLEET-01: the vehicle list screen at /vehicules — dense data table on desktop degrading to stacked cards below md (D-01), live status badges (D-02), the D-03 column set with server-side status filter + client-side text search, and full D-09 UI-state coverage — replacing the Phase 1 placeholder route.

Purpose: ROADMAP Phase 2 success criterion 1 — "User can view a list of all vehicles showing live status." Read-only per D-05: no create/edit/delete affordances anywhere on the screen. Visible to every authenticated role per D-08 — no client-side gating beyond the existing _authenticated guard.
Output: VehicleList component + tests, vendored table/select primitives, real /vehicules route, updated placeholder and router tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-fleet/02-RESEARCH.md
@.planning/phases/02-fleet/02-CONTEXT.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-UI-SPEC.md
@.planning/phases/02-fleet/02-01-SUMMARY.md
@src/features/fleet/queries.ts
@src/features/fleet/StatusBadge.tsx
@src/test/fixtures/fleet.ts
@src/routes/_authenticated/vehicules.tsx
@src/routes/_authenticated/placeholders.test.tsx
@src/app/router.test.tsx
@src/shared/ui/empty-state.tsx
@src/shared/i18n/useLocale.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor table/select primitives and build VehicleList</name>
  <files>src/shared/ui/table.tsx, src/shared/ui/select.tsx, src/features/fleet/VehicleList.tsx, src/features/fleet/VehicleList.test.tsx</files>
  <behavior>
    - Renders one row per fixture vehicle with plate, brand + model, translated status badge (all four enum values appear across the fixture set), locale-formatted mileage, translated fuel TYPE label, and agency name (ownerFixture store state)
    - Choosing a status in the filter causes a request whose URL carries status={value}; clearing back to "Tous les statuts" drops the param
    - Typing a plate fragment (or brand/model fragment) in the search input narrows visible rows with NO additional network request; a non-matching term shows the vehicles.noResults copy, not the EmptyState
    - While the query is pending, skeleton rows render; on a 500 handler override, the vehicles.loadError banner renders and clicking Réessayer refetches and recovers
    - With an empty vehicles array from MSW, EmptyState renders with the vehicles.empty.* keys
    - With useAuthStore agencies empty (non-admin), the agency column/field is not rendered
    - Each row's plate is a link whose href is /vehicules/{vehicle.id}
  </behavior>
  <action>
    1. Vendor primitives: run `npx shadcn@latest add table` then `npx shadcn@latest add select` from the repo root (components.json alias already targets `src/shared/ui/`). Inspect BOTH generated files before committing (01-02 sonner/next-themes precedent + research A1): imports must resolve to the installed consolidated `radix-ui` package and existing utils only; verify `git diff package.json` is empty — the CLI must not add npm dependencies. If `table` generation misbehaves, hand-write the ~20-line styled `<table>` wrapper components with existing tokens instead; if `select` misbehaves, fall back to the existing `dropdown-menu` (AgencySwitcher precedent). Using `select` over dropdown-menu is a discretion choice per 02-RESEARCH.md ("semantically better for a filter control").
    2. `src/features/fleet/VehicleList.tsx` implementing D-01/D-03/D-09:
       - Data: `useVehiclesQuery(status)` from plan 02-01 with local `useState<VehicleStatus | null>` for the status filter (server-side via ?status= — the API supports it; research resolution of D-03) and `useState("")` for the search term. Client-side text filter over plate/brand/model via `useMemo`, case-insensitive; no debounce infrastructure (unpaginated list, research Pitfall 6 — render ALL rows, no pagination UI, no client-side slicing).
       - Header: `t("vehicles.title")` heading, count line via the existing `vehicleCount` plural key with the FILTERED row count, search `Input` with `vehicles.searchPlaceholder`, status `Select` labelled `vehicles.statusFilterLabel` offering `vehicles.statusFilterAll` + the four `vehicles.status.*` options (option values from the VehicleStatus union — never hardcoded French).
       - Desktop (md+): dense table per 01-UI-SPEC density exceptions — row vertical padding 8px (sm), cell horizontal padding 12px. Columns per D-03: plate, vehicle (brand + model, with model_year appended when present), status (`<StatusBadge>`), mileage (`numeric-cell` utility class + `current_mileage.toLocaleString(locale)` + " km", locale from `useLocale()`), fuel type (`t(`vehicles.fuelType.${v.fuel_type}`)`), agency. The fuel column shows `fuel_type` — the only per-vehicle fuel datum the API has (research "Fuel level reality"); never reference a fuel-level property on a vehicle object.
       - Agency column: resolve names from `useAuthStore((s) => s.agencies)` by `agency_id`; when the agencies array is empty (non-admins — store is owner-only by design), omit the column entirely.
       - Mobile (<md): stacked Card per vehicle with the same data points — implement as `hidden md:block` table wrapper + `md:hidden` card stack (both in DOM; CSS controls visibility since jsdom can't evaluate media queries).
       - Navigation: plate cell (and card title) is a TanStack Router `Link` to `/vehicules/$vehicleId` with `params: { vehicleId: v.id }`.
       - States per D-09: pending → Skeleton rows shaped like the table; error → inline banner using `vehicles.loadError` + Button `vehicles.retry` calling `refetch()` (AppShell meError pattern); data empty → `<EmptyState titleKey="vehicles.empty.heading" descriptionKey="vehicles.empty.body" />`; populated-but-filter-empty → `vehicles.noResults` paragraph (distinct state).
       - Copy discipline (D-07): zero bare JSX string literals — every visible string goes through `t()` (the sole exception: raw data values like plate/VIN/mileage numbers and the literal "km" unit may render directly; prefer including the unit in the mileage formatting).
    3. `src/features/fleet/VehicleList.test.tsx`: cover every behavior line. Harness: render inside a fresh `QueryClientProvider` (retry: false) with MSW; seed `useAuthStore.setState` with ownerFixture agencies + a currentAgencyId, resetting in afterEach; for Link context, build a minimal memory-history router around the component (or reuse the real routeTree render pattern from router.test.tsx) — assert the plate link's href. Assert the status-filter request URL via a one-off `server.use` spy handler. For the error case override the vehicles handler with a 500 once, then let the retry hit the default handler.

    RED first: write VehicleList.test.tsx, watch it fail, then implement.
  </action>
  <verify>
    <automated>cd /Users/azizbelkacemi/Desktop/work-dev/wheelio-front && npx vitest run src/features/fleet && npx tsc -b</automated>
  </verify>
  <done>All behavior-block assertions pass; table+select vendored with no new npm dependencies; every D-09 state reachable and tested; no bare JSX copy literals.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the /vehicules placeholder route and update route tests</name>
  <files>src/routes/_authenticated/vehicules/index.tsx, src/routes/_authenticated/vehicules.tsx, src/routes/_authenticated/placeholders.test.tsx, src/app/router.test.tsx, src/routeTree.gen.ts</files>
  <action>
    Execute the placeholder swap exactly as 02-RESEARCH.md Pitfall 4 prescribes — deletion and creation in the SAME commit, because two files claiming /vehicules breaks route generation (which runs on every pretest/predev/prebuild hook):

    1. Create `src/routes/_authenticated/vehicules/index.tsx`: `createFileRoute("/_authenticated/vehicules/")` registering a thin page component that renders `<VehicleList />` from `@/features/fleet/VehicleList`. No loader — Phase 1 established component-level useQuery (research "Alternatives Considered"); do not introduce the loader idiom.
    2. DELETE `src/routes/_authenticated/vehicules.tsx` (the Phase 1 placeholder).
    3. Update `src/routes/_authenticated/placeholders.test.tsx`: remove the `VehiculesRoute` import and its `"/vehicules"` entry from `allPlaceholderRoutes`; the first two tests ("renders the FR copy" / "switches to EN live") currently sample the Vehicules route — switch them to `ClientsRoute` so the locale-switch coverage survives. The remaining placeholder routes keep their assertions unchanged.
    4. Run `npm run generate-routes` and confirm `src/routeTree.gen.ts` regenerates with a single `/vehicules` path (commit the regenerated file, matching repo convention).
    5. Extend `src/app/router.test.tsx` (full routeTree + memory history + MSW harness already in that file): after the authenticated-session setup, navigate to `/vehicules` and assert the real list screen renders — the `vehicles.title` heading "Véhicules" appears AND the placeholder's "Bientôt disponible" heading does not. The vehicles MSW handlers from plan 02-01 make the list resolve with fixture rows.
  </action>
  <verify>
    <automated>cd /Users/azizbelkacemi/Desktop/work-dev/wheelio-front && npm test && npm run build</automated>
  </verify>
  <done>Route generation yields exactly one /vehicules route rendering VehicleList; placeholder file deleted; placeholders.test.tsx and router.test.tsx updated; full suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser | plate/brand/model/notes strings rendered into table cells and cards |
| user input → component state | search term (client-side only, never sent to the API) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering (XSS) | VehicleList cells | medium | mitigate | React JSX auto-escaping; search term used only in a useMemo predicate, never as HTML |
| T-02-05 | Elevation of privilege | list visibility | low | transfer | backend authorize(levelRead) + RLS scope the returned rows; frontend renders only what the API returns and re-derives nothing (D-08) |

No package-manager installs (shadcn add vendors source files only) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `npm test` fully green including regenerated routes, updated placeholder tests, extended router test.
- `npm run build` green.
- Manual spot-check available via `npm run dev` (MSW off in dev — requires local backend) — not blocking; component tests are the gate.
</verification>

<success_criteria>
- ROADMAP Phase 2 criterion 1 satisfied against mocks: list of all vehicles with live status badges, filter + search, all UI states, responsive table/cards.
- Placeholder route fully retired without breaking route generation or existing tests.
</success_criteria>

<output>
Create `.planning/phases/02-fleet/02-02-SUMMARY.md` when done.
</output>
