---
phase: 03-clients
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-03]
files_modified:
  - src/features/customers/CustomerDetail.tsx
  - src/features/customers/CustomerDetail.test.tsx
  - src/routes/_authenticated/clients/$customerId.tsx
  - src/app/router.test.tsx
  - src/routeTree.gen.ts
autonomous: true
requirements: [CUST-01, CUST-02, CUST-03]

must_haves:
  truths:
    - "/clients/{id} shows the customer's fields — for an individual: full_name, identity doc type + number, license details; for a company: legal_name, rc/nif/nis — each optional field presence-guarded (omitempty absent, never rendered as 'undefined')"
    - "For a company, the detail lists its designated drivers (full_name + license_number + dates) from GET /customers/{id}/drivers; an individual shows no drivers section (D-03/D-05 scope)"
    - "An unknown or out-of-scope customer id renders a generic not-found state with a working back-to-list link — no crash, no forbidden-vs-missing distinction leaked"
    - "The customer + drivers queries fail independently — a drivers-query error shows an inline banner inside the drivers section and never blanks the page"
    - "The $customerId stub from 03-03 is replaced by the real CustomerDetail and the full phase suite (unit + tsc + build + playwright) is green"
  artifacts:
    - "src/features/customers/CustomerDetail.tsx"
    - "src/features/customers/CustomerDetail.test.tsx"
    - "src/routes/_authenticated/clients/$customerId.tsx"
  key_links:
    - "CustomerDetail composes useCustomerQuery(id) + useCustomerDriversQuery(id) from 03-01 — two parallel reads, no waterfall; drivers only requested/rendered for the company branch"
    - "$customerId.tsx (stubbed in 03-03 as the create-form navigation target) now registers CustomerDetail — the same stub-then-fill sequence 02-03 used for $vehicleId"
    - "Detail shows only what the customer endpoints expose — NO contract history (Phase 4), NO edit/delete affordances (scope fence)"
---

<objective>
Ship D-05: the customer detail screen at `/clients/$customerId` — identity/company fields plus the designated-drivers list for companies — and run the Phase 3 gate that proves all three CUST criteria end-to-end.

Purpose: closes the phase loop. The create form (03-03) navigates here on success and the list (03-02) links here per row, so this is where a user confirms the record they created or found. Read-only per the scope fences: only what the customer endpoints expose — no contract history (Phase 4), no edit/delete UI, no document upload. This plan also carries the phase gate: full unit suite + tsc + build + the migrated Playwright E2E must all be green before verification.
Output: CustomerDetail + tests, the real $customerId route (fills the 03-03 stub), a router integration assertion, and a green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-clients/03-RESEARCH.md
@.planning/phases/03-clients/03-CONTEXT.md
@.planning/phases/01-foundations-auth-shell-i18n-design-system/01-UI-SPEC.md
@.planning/phases/03-clients/03-01-PLAN.md
@src/features/fleet/VehicleDetail.tsx
@src/features/fleet/VehicleDetail.test.tsx
@src/features/customers/queries.ts
@src/types/customer.ts
@src/routes/_authenticated/clients/$customerId.tsx
@src/app/router.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CustomerDetail screen (fields + company drivers, parallel reads, not-found) + fill $customerId route</name>
  <files>src/features/customers/CustomerDetail.tsx, src/features/customers/CustomerDetail.test.tsx, src/routes/_authenticated/clients/$customerId.tsx, src/routeTree.gen.ts</files>
  <behavior>
    CustomerDetail.test.tsx (MSW + fresh QueryClient + memory router registering /clients):
    - individual fixture: renders full_name, identity doc type + number, license details; NO drivers section; a back-to-list link to /clients
    - company fixture: renders legal_name + rc/nif/nis and a drivers list (each driver's full_name + license_number) from GET /customers/{id}/drivers
    - omitempty guarding: a bare customer (optional keys absent) renders without any literal "undefined" and without crashing a formatter
    - unknown id (404): renders the generic not-found state + working back link; no forbidden-vs-missing distinction
    - drivers-query error (company): an inline banner inside the drivers section with retry; the customer fields above still render (independent failure)
    router assertion is added in Task 2's file set below via 03-04; here the component + route are proven through the memory router.
  </behavior>
  <action>Create `src/features/customers/CustomerDetail.tsx` modeled on `VehicleDetail.tsx`: compose `useCustomerQuery(id)` and `useCustomerDriversQuery(id)` (03-01) as two PARALLEL reads (no waterfall). While the customer query is pending, show a skeleton; on a 404 (use ky `isHTTPError` + status 404, as VehicleDetail does) render a single generic not-found state (never an access-denied variant — the backend 404s both nonexistent and out-of-scope, and the not-found copy keys already exist in `customers.detail.*`); on a non-404 error show the inline error banner + retry. On success render presence-guarded rows via a DetailRow helper (mirror VehicleDetail): for `type === "individual"` show full_name, identity_doc_type (i18n label), identity_doc_number, and license_number/dates when present; for `type === "company"` show legal_name, rc, nif, nis when present, plus phone/address when present. Render the designated-drivers section ONLY for the company branch (D-03/D-05): drive it from `useCustomerDriversQuery(id)` — loading skeleton, an inline error banner + retry that stays INSIDE the drivers card (never blanks the page, like VehicleDetail's contract card), an empty-drivers line when the list is empty, else each driver's full_name + license_number + optional dates. A back link points to `/clients`. Every label flows through i18n; raw data values render via React auto-escaping. Do NOT render contract history, edit/delete controls, or `archived_at` (scope fences + not in DTO).

Replace the 03-03 stub `src/routes/_authenticated/clients/$customerId.tsx` with the real route: `createFileRoute("/_authenticated/clients/$customerId")` reading `Route.useParams()` and rendering `<CustomerDetail customerId={customerId} />` (mirror `vehicules/$vehicleId.tsx`). Regenerate the route tree (routeTree.gen.ts gitignored; list, do not commit).</action>
  <verify>
    <automated>npx vitest run src/features/customers/CustomerDetail.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>CustomerDetail renders individual + company fields with presence-guarded optionals, lists company drivers with an independently-failing/retryable section, and shows a generic not-found for unknown ids; the $customerId route renders the real detail; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: router integration assertion + Phase 3 gate (full unit + tsc + build + playwright)</name>
  <files>src/app/router.test.tsx</files>
  <action>Add one integration assertion to `src/app/router.test.tsx` mirroring its `/vehicules/{id}` test: navigating to `/clients/{id}` through the real generated route tree resolves CustomerDetail (assert a field the detail renders for a known fixture, e.g. the customer's display name heading), proving list/form → detail routing works end-to-end via the committed route tree + the 03-01 customer MSW handlers.

Then run the FULL Phase 3 gate and confirm green: the entire unit suite (`npx vitest run`), a strict type-check (`npx tsc --noEmit`), a production build (`npm run build`), and the Playwright E2E suite (`npx playwright test`) — the last must show all 4 auth.spec.ts tests green with the placeholder assertions migrated onto /etats-des-lieux (03-02). Fix any regression surfaced (e.g. an i18n key gap or a route-tree drift) before declaring the phase gate passed. This is the phase's Nyquist phase-gate sampling point (research Validation Architecture: full unit suite + playwright green before /gsd-verify-work).</action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run build && npx playwright test</automated>
  </verify>
  <done>The router test proves /clients/{id} resolves CustomerDetail; the full unit suite, tsc, build, and all 4 Playwright E2E tests are green — the Phase 3 gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /customers/{id}, GET /customers/{id}/drivers | customer + driver PII returned and rendered here |
| customer existence | a 404 must not distinguish "missing" from "out-of-scope" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Information Disclosure | not-found state | medium | mitigate | One generic not-found for both nonexistent and out-of-scope ids — never an access-denied variant that would confirm existence (mirror VehicleDetail) |
| T-03-12 | Information Disclosure | driver/customer PII rendering | medium | mitigate | Render only; no logging of payloads; React auto-escapes all values |
| T-03-13 | Availability | independent query failure | low | mitigate | Customer + drivers queries fail independently; a drivers error stays inside its card and does not blank the page |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan |
</threat_model>

<verification>
- `npx vitest run` — full unit suite green
- `npx tsc --noEmit` and `npm run build` — clean
- `npx playwright test` — all 4 auth.spec.ts tests green (placeholder assertions on /etats-des-lieux)
- Human end-of-phase check (human_verify_mode=end-of-phase): confirm via /gsd-verify-work that (1) an individual customer can be created with identity doc + license, (2) a company customer can be created with RC/NIF/NIS + one or more drivers, and (3) an existing customer can be found via search and opened.
</verification>

<success_criteria>
The customer detail screen renders individual/company fields + company drivers, handles not-found and independent query failures, and closes the create/find loop; the full Phase 3 gate (unit + tsc + build + playwright) is green.
</success_criteria>

<output>
Create `.planning/phases/03-clients/03-04-SUMMARY.md` when done.
</output>
