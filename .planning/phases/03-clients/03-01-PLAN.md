---
phase: 03-clients
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/customer.ts
  - src/shared/auth/permissions.ts
  - src/shared/auth/permissions.test.ts
  - src/test/mocks/handlers.ts
  - src/test/mocks/handlers.test.ts
  - src/test/fixtures/customers.ts
  - src/features/customers/api.ts
  - src/features/customers/queries.ts
  - src/features/customers/queries.test.tsx
  - src/shared/i18n/fr/common.json
  - src/shared/i18n/en/common.json
autonomous: true
requirements: [CUST-01, CUST-02, CUST-03]

must_haves:
  truths:
    - "src/types/customer.ts mirrors customer_dto.go 1:1 (snake_case JSON tags, every omitempty field optional ?:, license dates string|null) so any contract drift fails tsc"
    - "hasOrgRole(scope, min) returns true for an org admin OR any user holding min-or-above in ANY agency, and false otherwise — the org-wide axis, never the per-agency canOperate axis (D-08)"
    - "MSW GET /customers parses ?q= and filters fixtures over name + CIN + RC; POST /customers returns 201 echoing the body with a generated id/timestamps; POST /customers/:id/drivers returns 201; GET /customers/:id returns 200 or 404; GET /customers/:id/drivers returns 200"
    - "queries.ts exposes useCustomersQuery(q) keyed ['customers','list',{q}], useCustomerQuery(id) keyed ['customers','detail',id], useCustomerDriversQuery(id) — NONE carrying currentAgencyId (customers are org-scoped, D-07)"
    - "The customers.* i18n namespace exists in FR and EN with identical key sets (zero bare-literal gaps for list, create, detail, drivers, and errors copy)"
  artifacts:
    - "src/types/customer.ts"
    - "src/shared/auth/permissions.ts"
    - "src/test/mocks/handlers.ts"
    - "src/test/fixtures/customers.ts"
    - "src/features/customers/api.ts"
    - "src/features/customers/queries.ts"
    - "src/shared/i18n/fr/common.json"
    - "src/shared/i18n/en/common.json"
  key_links:
    - "hasOrgRole is a literal port of Scope.HasOrgRole (wheelio-api internal/domain/identity/scope.go:51) — same admin shortcut + AtLeast(min)-over-any-agency semantics"
    - "api.ts issues every call through the shared @/shared/api/client ky instance (single-flight refresh + /v1 prefix) — never a second HTTP client"
    - "MSW handler and fixture bodies are typed against src/types/customer.ts so a mismatch with the Go contract is a compile error, not a runtime surprise"
---

<objective>
Ship the Phase 3 data/contract foundation every later customer plan builds on: the `src/types/customer.ts` DTO mirror, the org-scoped `hasOrgRole` permission helper, MSW customer handlers + fixtures, the `src/features/customers/{api,queries}.ts` data layer, and the full FR/EN `customers.*` i18n namespace.

Purpose: this is the Wave-1 backbone for CUST-01/CUST-02/CUST-03. It contributes no screen; it makes the list (03-02), create form (03-03), and detail (03-04) plans pure composition. The single most consequential decision it encodes is the org-scope authz axis (D-08): customers have NO agency boundary, so the gate is `hasOrgRole`, and `currentAgencyId` never enters a customer query key.
Output: customer types, hasOrgRole helper + tests, MSW handlers + fixtures, api.ts + queries.ts + hook tests, customers.* FR/EN copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-clients/03-RESEARCH.md
@.planning/phases/03-clients/03-CONTEXT.md
@.planning/phases/02-fleet/02-01-SUMMARY.md
@src/types/fleet.ts
@src/shared/auth/permissions.ts
@src/shared/auth/permissions.test.ts
@src/features/fleet/api.ts
@src/features/fleet/queries.ts
@src/features/fleet/queries.test.tsx
@src/test/mocks/handlers.ts
@src/test/fixtures/fleet.ts
@src/test/fixtures/scope.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hasOrgRole org-scope permission helper (literal port of Scope.HasOrgRole)</name>
  <files>src/shared/auth/permissions.ts, src/shared/auth/permissions.test.ts</files>
  <behavior>
    Add and test hasOrgRole(scope: Scope, min: AgencyRole): boolean. Cases:
    - org admin (owner/admin), empty agencyRoles → true for min "viewer" and min "agent" (admin shortcut, ownerFixture)
    - member with agent in one agency (agentFixture) → true for "viewer", true for "agent"
    - member with manager in one agency (managerFixture) → true for "viewer","agent","manager"
    - member with ONLY viewer in one agency (inline scope) → true for "viewer", FALSE for "agent"
    - member with EMPTY agencyRoles and orgRole "member" (inline scope) → FALSE for "viewer" and "agent"
    - the existing canOperate/canRead/canManage tests remain green (helper is additive)
  </behavior>
  <action>Add a `hasOrgRole(scope, min)` export to permissions.ts implementing the org-wide axis: return true when `isOrgAdmin(scope)` is true, otherwise return whether ANY value in `scope.agencyRoles` has `RANK[role] >= RANK[min]`. This is a verbatim port of wheelio-api `internal/domain/identity/scope.go:51` `Scope.HasOrgRole` (admin shortcut, then AtLeast(min) over every agency membership) — mirror it exactly per D-08 and cite the Go file:line in the doc comment. Keep the existing `canOperate(scope, agencyId)` / `canRead` / `canManage` untouched; note in the doc comment that those are the per-AGENCY axis and MUST NOT be used to gate customer records (customers are org-scoped, no agencyID). Reuse the existing `isOrgAdmin`, `RANK`, and `AgencyRole` already in the file — do not redefine them. In permissions.test.ts, extend the suite with a `describe("hasOrgRole", ...)` covering every case in the behavior block; build the viewer-only and empty-membership member scopes inline (no such fixture exists) as `Scope` objects.</action>
  <verify>
    <automated>npx vitest run src/shared/auth/permissions.test.ts</automated>
  </verify>
  <done>hasOrgRole exported and covered; all permissions tests pass; the read gate hasOrgRole(scope,"viewer") and write gate hasOrgRole(scope,"agent") behave as the backend's HasOrgRole does.</done>
</task>

<task type="auto">
  <name>Task 2: customer DTO mirror + MSW customer handlers + fixtures</name>
  <files>src/types/customer.ts, src/test/mocks/handlers.ts, src/test/mocks/handlers.test.ts, src/test/fixtures/customers.ts</files>
  <action>Create `src/types/customer.ts` mirroring wheelio-api `internal/adapter/httpapi/customer_dto.go` verbatim, following the exact idiom of `src/types/fleet.ts` (doc comment naming the Go source as the single source of truth, omitempty rule). Export: `CustomerType = "individual" | "company"` (customer.go:20-22); `IdentityDocType = "cin" | "passport"` (customer.go:89-101); `CustomerResponse` (customer_dto.go:69-89 — id, type, created_at, updated_at required; full_name, identity_doc_type, identity_doc_number, license_number, legal_name, rc, nif, nis, phone, address all optional `?:`; license_issued_at and license_valid_until as `string | null` optional since they are `*string`); `DriverResponse` (customer_dto.go:91-100 — id, customer_id, full_name, license_number, created_at, updated_at required; the two license dates `string | null` optional). Also export request-body types `CreateCustomerBody` (mirrors createCustomerRequest, customer_dto.go:18-35) and `CreateDriverBody` (mirrors createDriverRequest, customer_dto.go:53-58) so both api.ts and the MSW handlers type against them. Do NOT model `archived_at` — it is not in the response DTO (research omitempty note). Add no format regex-carrying fields; these are plain strings.

Create `src/test/fixtures/customers.ts` typed against those types, mirroring the structure of `src/test/fixtures/fleet.ts`: at least one individual with identity_doc_type "cin" + identity_doc_number + license fields; one individual with "passport" and NO identity_doc_number (passport-optional path); one company with legal_name + rc + nif + nis; one "bare" customer with every omitempty field ABSENT (keys not present, never null/empty). Add two driver fixtures belonging to the company customer, plus an exported `customerFixtures` array and a `driversByCustomerId` lookup (or equivalent) the handlers can filter over. Customers are org-scoped, so fixtures carry NO agency_id.

Extend `src/test/mocks/handlers.ts` with a "// ---- Customers (Phase 3) ----" section, following the existing MSW v2 convention (parse `request.url` searchParams; path-only matching): GET `${API_URL}/customers` reads `?q`, and when present filters `customerFixtures` case-insensitively over full_name, legal_name, identity_doc_number (CIN) and rc, returning the full slice when q is empty/absent (200, typed `CustomerResponse[]`); POST `${API_URL}/customers` reads the JSON body, returns 201 with a `CustomerResponse` echoing the body plus a generated id + created_at/updated_at; GET `${API_URL}/customers/:customerId` returns 200 with the matching fixture or 404 `{ message: "customer not found" }`; POST `${API_URL}/customers/:customerId/drivers` returns 201 with a `DriverResponse` echoing the body + generated id + the path customer_id (404 `{ message: "unknown customer" }` when the parent id matches no fixture — the backend maps this to 400, but a 404 keeps the mock honest for the detail read; align the status with research: parent-missing on driver create is a 400 "unknown customer" — return 400 to mirror the real service, customer_handler.go/service.go); GET `${API_URL}/customers/:customerId/drivers` returns 200 with the customer's drivers. Add customer smoke assertions to `src/test/mocks/handlers.test.ts` mirroring the existing fleet handler tests: q-filter narrows results, POST /customers echoes + 201, GET /customers/:id 404 path.</action>
  <verify>
    <automated>npx vitest run src/test/mocks/handlers.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>customer.ts compiles as a strict mirror of the Go DTO; fixtures + handlers are typed against it (drift = compile error); customer handler tests pass; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: customers api.ts + queries.ts (org-scoped, no agencyId) + customers.* i18n</name>
  <files>src/features/customers/api.ts, src/features/customers/queries.ts, src/features/customers/queries.test.tsx, src/shared/i18n/fr/common.json, src/shared/i18n/en/common.json</files>
  <behavior>
    queries.test.tsx (MSW + fresh QueryClient, mirroring fleet/queries.test.tsx):
    - useCustomersQuery("") fetches GET /customers and returns the full fixture slice; key is ["customers","list",{ q: "" }]
    - useCustomersQuery("<term>") sends ?q=<term> and returns only matching fixtures
    - useCustomerQuery(id) fetches GET /customers/{id} under ["customers","detail",id]
    - useCustomerDriversQuery(id) fetches GET /customers/{id}/drivers under ["customers","detail",id,"drivers"]
    - NO request carries an agency_id param and NO key contains currentAgencyId, even when the auth store has a currentAgencyId set (org-scoped invariant)
  </behavior>
  <action>Create `src/features/customers/api.ts` as thin functions over the shared `api` ky client (mirror `src/features/fleet/api.ts`, same doc-comment discipline): `fetchCustomers(q: string)` building URLSearchParams and setting `q` only when `q.trim() !== ""`; `fetchCustomer(id)`; `fetchCustomerDrivers(id)`; `createCustomer(body: CreateCustomerBody)` POSTing to "customers"; `createDriver(customerId, body: CreateDriverBody)` POSTing to `customers/${customerId}/drivers`. All return typed `.json<...>()`. Never instantiate a second client.

Create `src/features/customers/queries.ts` with `useCustomersQuery(q)`, `useCustomerQuery(id)`, `useCustomerDriversQuery(id)` using the keys in the behavior block. Critically, do NOT read `currentAgencyId` from the auth store and do NOT add it to any key or param — customers are org-scoped (D-07, research Scoping section). Add a doc comment stating this contrast with fleet's agency-keyed list, so a future reader does not "fix" it by adding agencyId. The create-then-attach mutation is intentionally NOT here — it lives in 03-03's mutations.ts. Write queries.test.tsx covering the behavior block.

Add the full `customers.*` namespace to BOTH `src/shared/i18n/fr/common.json` and `src/shared/i18n/en/common.json` with IDENTICAL key sets (FR is the hard default, EN mirrors it — D-06). Provide the complete namespace the later plans consume so no bare JSX literal is ever needed: `title`, `searchPlaceholder`, `noResults`, `loadError`, `retry`, `columns.{name,type,identityDoc,phone}`, `type.{individual,company}`, `identityDocType.{cin,passport}`, `empty.{heading,body}`, `create.{cta,title,submit,typeLegend,individualLabel,companyLabel,success}`, `fields.{fullName,legalName,identityDocType,identityDocNumber,rc,nif,nis,licenseNumber,licenseIssuedAt,licenseValidUntil,phone,address}`, `drivers.{title,add,remove,fullName,licenseNumber,empty}`, `errors.{cinRequired,licenseDateOrder,createFailed,driverFailed,partialSuccess}`, `detail.{backToList,identityTitle,companyTitle,licenseTitle,driversTitle,notFoundHeading,notFoundBody}`. Add a top-level `customerCount_one` / `customerCount_other` plural pair mirroring the existing `vehicleCount_one`/`vehicleCount_other`. Do NOT invent wilaya/legal-form option sets (D-09) — there are no such enums. Keep the FR copy natural Algerian-French business register; EN as the confirmed mirror.</action>
  <verify>
    <automated>npx vitest run src/features/customers/queries.test.tsx src/shared/i18n && npx tsc --noEmit</automated>
  </verify>
  <done>The three read hooks work against MSW with org-scoped keys (no agencyId anywhere); customers.* exists in FR+EN with matching key sets and a customerCount plural pair; hook tests + i18n tests + tsc all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → wheelio-api (customer endpoints) | customer PII (identity doc, license, RC/NIF/NIS) crosses here over the shared ky client |
| client role gate (hasOrgRole) → backend authz | client mirror is cosmetic; backend re-enforces HasOrgRole on every request (403) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | hasOrgRole helper vs canOperate | high | mitigate | Port HasOrgRole exactly (scope.go:51); document that per-agency canOperate is the WRONG axis for customers; backend 403 is the real boundary (server re-checks) |
| T-03-02 | Information Disclosure | customer PII in api/query layer | medium | mitigate | Never console.log request/response bodies; PII stays in memory + query cache only (research Security Domain) |
| T-03-03 | Tampering | contract drift in customer.ts | medium | mitigate | Type MSW handler + fixture bodies against src/types/customer.ts so Go-contract drift fails tsc, not at runtime |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (research Package Legitimacy Audit: none) — nothing to slopcheck |
</threat_model>

<verification>
- `npx vitest run src/shared/auth src/features/customers src/test/mocks src/shared/i18n` — all green
- `npx tsc --noEmit` — clean (proves the DTO mirror + typed fixtures/handlers)
- No customer query key or request param contains `agency_id` / `currentAgencyId` (grep the customers feature dir to confirm the org-scoped invariant)
</verification>

<success_criteria>
hasOrgRole ported + tested; customer.ts is a strict Go-contract mirror; MSW customer handlers + fixtures typed and passing; three read hooks org-scoped with correct keys; customers.* FR/EN namespace complete with matching keys; full suite + tsc + build green.
</success_criteria>

<output>
Create `.planning/phases/03-clients/03-01-SUMMARY.md` when done.
</output>
